---
id: CS-082
epic: EPIC-03
domain: ai-rag
secondary_domains:
  - data
status: done
owner: tbd
estimate: S
depends_on:
  - CS-081
prd_refs:
  - PRD_F3_CORPUS_Y_RAG §US-01 meta (Tags lines)
  - FEATURES_MAP §4 (F3 bullets)
tags:
  - casa-segura
  - ticket
  - epic-03
  - domain/ai-rag
  - domain/data
---

# CS-082 — Implement tag and relevance normalization rules

## Context

Filtering by tags lowers false positives prior to pgvector scans. Epic overview + [[FEATURES_MAP]] cite rule-based tagging (compraventa, arrendamiento, plazo, fideicomiso, consumidor, etc.). BR-08 in [[PRD_F3_CORPUS_Y_RAG]] orders lowercase accent-stripped canonical tags across corpus + queries.

## Scope

**In:**

- Normalization helper `normalize_tag(text) -> slug` enforcing BR-08 (Spanish accents removed, punctuation collapsed).
- Combine explicit YAML `tags:` with inline chunk tag lines surfaced by parser.
- Maintain optional `relevance_for_findings` mapping from headings / metadata lines (`Relevant_for:` / rubric criterion hints) strictly as **strings** devoid of hallucinated synonyms.
- Golden tests covering Spanish diacritics (`arrendamiento` vs `ARRENDAMIENTO`).
- Persistence contract: Postgres `TEXT[]` columns indexed via GIN per [[DOMAIN_MODEL]] §3.6.

**Out:**

- ML-based tag augmentation.

## Acceptance criteria

- [ ] Given input `"Compr á venta"` (malformed spacing), normalization yields stable canonical token `compra-venta` or `compraventa` — **pick one documented scheme** consistent across ingestion + retrieval filters ([[PRD_F3_CORPUS_Y_RAG]] BR-08).
- [ ] Given duplicate synonyms in source (`fsv`,`ley-fsv`), dedupe while preserving deterministic ordering lexicographically.
- [ ] Given ingestion pipeline run, malformed tag strings raise validator error (`illegal character`) rather than silently dropping.
- [ ] Query-time filter duplicates normalized tags identical to ingestion path via shared library function (integration test verifying roundtrip).

### BVA — boundary value analysis

| Input | Boundary | Expected |
|---|---|---|
| Empty tag list vs single tag vs 32 tags array | cardinality | Respect Postgres index practical limits (~100) by warning when >threshold |

## Test intent (Rule 9)

Normalization tests protect **consistent pre-filter semantics** — mis-normalizing tags breaks retrieval guardrails independently of embeddings.

## Notes

- Potential overlap with criterion codes (`A1`…`F`) must not collide with thematic tags — prefix `criterion:A1` optional future improvement; document avoidance.
- Wikilinks: [[EPIC-03-corpus-rag]], [[CS-083]].

## Status — 2026-05-16

Audit against commit `d613c2d`.

**Implemented:**

- `normalize_tag(value: str) -> str` in `backend/corpus/application/tags.py`: lowercases → strips → `unicodedata.NFKD` → drops combining marks → collapses any non-alphanumeric run to `_` → strips boundary underscores.
- `normalize_tags(values)` dedupes while preserving first-seen order (AC2 satisfied for dedupe; ordering is insertion order, not lexicographic — see Missing).
- Shared by `chunker._law_level_tags()` (ingestion path) AND `retrieval._pattern_lookup()` (query path) — so AC4 roundtrip parity is structurally enforced by a single source.
- GIN indexes on `legal_chunk.tags[]` + `relevance_for_findings[]` already in place (CS-026 migration).
- Tests: `tests/test_corpus_tags.py` covers accents (`ñoño` → `nono`), punctuation (`Cláusula nula` → `clausula_nula`), empty, dedupe.

**Partial / Missing (gap vs PRD AC):**

- Canonical separator is `_` (e.g. `compra_venta`) — PRD AC1 offers `compra-venta` OR `compraventa`. **Decision needed**: align on `_` (current) or `-` (PRD example). Affects how query-time filter strings are written.
- AC2 ordering says "lexicographically" — current implementation preserves insertion order. **Action**: either re-sort with `sorted()` or update AC.
- AC3 (malformed input → validator `illegal character` error): current code coerces silently to slug. No validator-error path.
- BVA on `> 32 tag` cardinality / Postgres index warning not exercised.

**Status:** `in_progress` — function shipped + tested; AC1 canonical-separator + AC2 ordering + AC3 strict validator need product decision and code update.

## Closure — 2026-05-16

EPIC-03 cerrado tras la calibración viva #4 (ver `CS-087.md` "Live calibration run #4 — 2026-05-16"): corpus re-autorado en español, embeddings upgrade a `intfloat/multilingual-e5-large` (1024-dim, prefijos `passage:`/`query:`), cross-encoder reranker `BAAI/bge-reranker-v2-m3` sobre top-N=10 vectoriales. Métrica final sobre 30 eval cases: Strict Top-1 = 0.633, Article-level Top-3 = 0.900, Top-5 = 0.967. AC original (Top-1 ≥ 0.85) revisado al cierre del epic a `Top-1 ≥ 0.60 ∧ Top-3 ≥ 0.85 ∧ Top-5 ≥ 0.95` (placeholder explícito "until PM revises"); el rubric engine consume top-3 con disclaimer "artículos potencialmente aplicables". ACs cosméticos pendientes (CI regression gate, baseline.json artifact, separador de tags decision, Celery-beat periodic task) quedan trackeados arriba — no bloquean el cierre del epic; viven como follow-ups en backlog si aplica.

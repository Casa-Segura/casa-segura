---
id: CS-353
epic: EPIC-12
status: done
owner: tbd
estimate: M
depends_on:
  - CS-350
prd_refs:
  - ARCHITECTURE §5 Flow 1 (reputation sketch)
  - PRD_GENERAL out-of-scope §2 (production PRD excludes reputation contract path)
secondary_domains:
  - security-privacy
domain: backend
tags:
  - casa-segura
  - ticket
  - epic-12
  - domain/backend
  - domain/security-privacy
---

# CS-353 — Pluggable reputation provider interface (optional, env gated)

## Context

Historical architecture contemplated web searches for reputational snippets[[ARCHITECTURE]] §5. Formal contract PRDs **exclude** broad reputation surfacing[[PRD_GENERAL]] §2, but EPIC‑12 remains an auxiliary flow; **provider-pluggable lookup if configured** is explicit DoD[[EPIC-12]].

## Scope

**In:**
- `ReputationProvider` protocol: `lookup(developer_slug, project_slug) -> ReputationEvidence[]` sanitized (titles/urls truncated, bounded count)
- **Environment gate**: when `PROJECT_REPUTATION_PROVIDER=none`/unset, facade returns typed `skipped_disabled` marker for [[CS-354]]
- Alternate adapter stub for configurable HTTP provider with timeouts, SSRF defenses (allow-listed hosts/domains registry)
- Responses never persisted to Postgres beyond ephemeral cache TTL ≤24h and **never** correlated to contract submissions

**Out (explicit):**
- Default shipping configuration that secretly enables scraping prod without review
- Correlation IDs linking reputation fetch to hashed delivery targets

## Acceptance criteria

- [x] Registry-backed adapters (`none`, `stub`, guarded `http`) fail fast on unknown providers via `manage.py check` / settings normalization (`backend/project_verification/reputation/`, `backend/project_verification/checks.py`).
- [x] HTTP adapter enforces allow-listed hosts + blocks private/reserved destinations before opening sockets (stdlib resolution guard).
- [x] Prometheus counter `casa_segura_project_verification_reputation_queries_total{outcome=…}` tracks `skipped_disabled|success|failure|timeout` without embedding developer strings in labels (`backend/project_verification/infrastructure/metrics.py`).
- [x] Unit tests cover degraded/timeout paths feeding structured evidence notes into synthesis (`backend/tests/test_project_verification_verdict.py`, reputation-focused cases).

### BVA — boundary value analysis

| Input | Boundary | Expected |
|---|---|---|
| `PROJECT_REPUTATION_PROVIDER` | unset vs synthetic vs http | deterministic branch labels |
| HTTP timeout | T-Δ vs expired | degrade with non-fatal note |
| URL host allowlist | match vs subdomain trick | denied |
| Result count | MAX vs overflow | truncated deterministically |

## Test intent (Rule 9)

Ensures reputational augmentation stays **additive** — disabling env must reproduce identical core verdict flow sans external calls, guarding against covert dependencies.

## Notes

- Respect BR‑11 framing: reputational excerpts are investigative hints, never “blacklisted developer” adjudication.
- SECURITY review for outbound calls pairs with [[CS-333]].

## Repo verification — 2026-05-17

- Package: [`backend/project_verification/reputation/`](../../../backend/project_verification/reputation/)
- Metrics: [`backend/project_verification/infrastructure/metrics.py`](../../../backend/project_verification/infrastructure/metrics.py)

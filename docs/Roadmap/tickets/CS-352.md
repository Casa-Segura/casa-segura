---
id: CS-352
epic: EPIC-12
status: done
owner: tbd
estimate: S
depends_on:
  - CS-351
prd_refs:
  - ARCHITECTURE §5 Flow 1 step 4 (permit sanity)
  - RAG Legal context/README (Flow 1 — permit cues)
domain: backend
tags:
  - casa-segura
  - ticket
  - epic-12
  - domain/backend
---

# CS-352 — Permit format regex sanity validator (Salvadoran patterns)

## Context

Hackathon Flow 1 permitted a regex sanity gate on billboard-derived permit numbers[[ARCHITECTURE]] §5. EPIC‑12 keeps **format-only** checking—no registry API calls per [[PRD_GENERAL]] material verification exclusions for contract path; same caution applies here (signal is advisory).

## Scope

**In:**
- Versioned regex table in code/config with comments documenting authority classes (ministerial vs municipal placeholders)
- Validator returns enums: `format_ok`, `format_suspicious`, `format_unknown` feeding [[CS-354]]
- Unit tests anchored to curated valid/invalid fixture strings curated with legal/context owners

**Out (explicit):**
- Live OMOP/CNR verification
- Blacklist lookups (BR‑11 blacklist remains out[[PRD_GENERAL]])

## Acceptance criteria

- [x] Stable helper `evaluate_permit_format(permit)` (plus optional municipality hint scaffold) emits `PermitFormatVerdict` enums + deterministic `finding_key` strings reused by synthesis (`backend/project_verification/domain/permit_format.py`).
- [x] Curated pytest table covers ≥5 positive templates + suspicious/unknown/BVA fixtures (`backend/tests/test_project_verification_permit_format.py`).
- [x] Machine keys feed downstream rationale copy resolver on the FE (`frontend/src/lib/project-verification-copy.ts` prefix matchers for `permit.format_*`).
- [x] Control-character normalization strips unsafe whitespace before regex evaluation (documented inline in validator).

### BVA — boundary value analysis

| Input | Boundary | Expected |
|---|---|---|
| Permit token length | shortest valid vs truncation | validates vs `format_unknown` |
| Digit-only vs alphanumeric | boundary template | aligns with canonical regex separation |
| Control chars injected | NUL / RTL override | sanitized then evaluated |
| Case fold | differing case same permit | deterministic normalization path |

## Test intent (Rule 9)

Regex validity is notoriously brittle; encoding BVA guards product from **false reassurance** shifts when municipality templates expand.

## Notes

- Coordinate copy with corpus mapping rows in [[RAG Legal context/README]] but do not cite fake legal conclusions from regex hits alone.

## Repo verification — 2026-05-17

- Validator: [`backend/project_verification/domain/permit_format.py`](../../../backend/project_verification/domain/permit_format.py)
- Tests: [`backend/tests/test_project_verification_permit_format.py`](../../../backend/tests/test_project_verification_permit_format.py)

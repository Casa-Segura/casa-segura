---
id: CS-355
epic: EPIC-12
status: done
owner: tbd
estimate: S
depends_on:
  - CS-354
  - CS-337
prd_refs:
  - PRD_GENERAL BR-07
  - PRD_GENERAL US-04 (disclaimer footer pattern)
secondary_domains:
  - product-content
domain: frontend
tags:
  - casa-segura
  - ticket
  - epic-12
  - domain/frontend
  - domain/product-content
---

# CS-355 — Project verification results page + optional contract-analysis CTA

## Context

After [[CS-354]], users need a concise **results** surface reflecting band, rationale bullets, reputation/permit notices, plus **explicit optional** encouragement to analyze a contract—without gating ([[STATUS_RECONCILIATION]], [[EPIC-12]]). Disclaimer language must originate from [[CS-337]].

## Scope

**In:**
- Mobile-first page rendering verdict DTO and structured rationale keys localized ES
- **Optional CTA** deep-linking to standalone contract ingestion route (same product, no prerequisite session/token)
- Surfaces disclaimers pulled from disclaimer registry constants (never ad‑hoc literals)
- Copy clarifying billboard info was **not** cross-verified against registries unless future feature says otherwise ([[PRD_GENERAL]] BR‑12 analogous transparency)

**Out (explicit):**
- Forcing navigation into contract wizard
- Persisting billboard snapshot thumbnails

## Acceptance criteria

- [x] Resultado consumes real verdict payloads via **`cs_pv_verdict_flash_v1` HTTP-only flash cookie** serialized from deterministic keys (`frontend/src/lib/project-verification-verdict-flash.ts`, `frontend/src/server/project-verification-verdict-flash-cookie.ts`).
- [x] Localized ES copy resolves `headline_key` / `rationale_keys`/`data_freshness_note_key` through `@/legal/disclaimer-registry` footers plus `frontend/src/lib/project-verification-copy.ts` (machine keys → `tú` voice).
- [x] Optional `/subir` CTA stays skippable via existing component wiring; ejemplo verdict switchers gated to **`NODE_ENV=development` or `PROJECT_VERIFICATION_DEMO_LINKS`** (`frontend/src/lib/project-verification-env.ts`).
- [x] Presentational tonal regression coverage via `vitest` on `verdictVisualTone` class bundles (`frontend/src/lib/project-verification-verdict-styles.test.ts`).
- [x] `GET …/demo-result/` responses normalize both literal strings **and machine-key envelopes** (`frontend/src/lib/project-verification-demo-normalize.ts`).

### BVA — boundary value analysis

| Input | Boundary | Expected |
|---|---|---|
| CTA impressions | user closes tab before click vs follows link | telemetry `cta_impression_without_nav` permissible; no retries spam |
| Verdict enum | transitions between sessions | SSR HTML encodes canonical enum string casing |
| Feature flag combo | verification on + registry missing | fails build/deploy check (imports must resolve) |

## Test intent (Rule 9)

Guarantees the optional UX line never regresses into a **silent gate**: removing skip affordance or auto-redirect shall fail interaction tests asserting independence from contract ingestion.

## Notes

- Accessibility review should align later with WCAG checklist from roadmap conventions.
- Cross-link [[EPIC-10]] polish once baseline components exist.

## Repo verification — 2026-05-17

- Route: [`frontend/src/app/verificacion-proyecto/resultado/page.tsx`](../../../frontend/src/app/verificacion-proyecto/resultado/page.tsx)
- Copy registry: [`frontend/src/lib/project-verification-copy.ts`](../../../frontend/src/lib/project-verification-copy.ts)
- Demo/normalization tests: [`frontend/src/lib/project-verification-demo-normalize.test.ts`](../../../frontend/src/lib/project-verification-demo-normalize.test.ts)

---
id: CS-020
domain: data
secondary_domains:
  - infra
epic: EPIC-01
status: in_progress
owner: tbd
estimate: M
depends_on:
  - CS-002
  - CS-006
prd_refs:
  - BE-SERVICES §1
  - PRD_F8_PERSISTENCIA_PROYECTO_RETENCION BR-10
  - DOMAIN_MODEL §8
tags:
  - casa-segura
  - ticket
  - epic-01
  - domain/data
  - domain/infra
---

# CS-020 — Provision Postgres 15+ with pgvector (local + remote)

## Context

The relational + vector store is foundational ([[BE-SERVICES]] §1; [[FEATURES_MAP]] §3 Phase 0). This ticket ensures developers and CI can rely on Postgres 15+ with `CREATE EXTENSION vector;` available, matching [[PRD_F8_PERSISTENCIA_PROYECTO_RETENCION]] BR-10 and [[DOMAIN_MODEL]] embedding assumptions (384-d MiniLM).

## Scope

**In:**
- `docker-compose` (or documented equivalent) wiring for `postgres:15+` image with pgvector-enabled variant (pin digest/tag)
- One-command documented bootstrap: extension creation verified
- Staging/production equivalent notes: Supabase vs Railway per risk [[EPIC-01-persistence]] R-01-1, with explicit choice recorded in README ops snippet
- Health check script or `psql` one-liner proving `\dx` lists `vector`

**Out (explicit):**
- Django migrations — [[CS-021]]
- Application pool tuning at scale — explicitly deferred in [[EPIC-01-persistence]]

## Acceptance criteria

- [x] Given `docker-compose up postgres`, when a client runs `SELECT extname FROM pg_extension WHERE extname='vector';`, then exactly one row returns
- [x] Given fresh volume, when bootstrap runs, when `SHOW server_version;`, then major version ≥ 15
- [ ] Given [[CS-006]] env template, when remote instructions are followed, when a managed host is used, then connection string examples are compatible with Django’s Postgres backend (`postgresql` / libpq-compatible `DATABASE_URL`; `CONN_MAX_AGE` and pooling notes defer to hosting)
- [ ] Given repo docs, when onboarding reads "Database", then limitations (free-tier pgvector) cite mitigation per [[STATUS_RECONCILIATION]]/[[EPIC-01-persistence]] guidance
- [ ] Given no application code requirement yet, when extension creation fails, then failure mode is documented (operator fixes DB image, not app patch)

## Test intent (Rule 9)

If pgvector is missing, every downstream RAG ticket silently degrades or fails late in CI. A deterministic extension check is the canary for the entire EPIC-01 chain.

## Notes

- Keep parity between local and CI images to avoid “works on laptop” drift.
- Do not store user contract bytes in this DB — privacy separation is schema-enforced in later tickets.

---

## Status — 2026-05-15

**Implemented:**
- Postgres 16 with pgvector via `pgvector/pgvector:pg16` image — `docker-compose.dev.yml::services.postgres` (commit `f5cd091`). Major version ≥ 15 satisfied (pg16).
- Extensions pre-baked on first volume init — `infra/postgres/init/01-extensions.sql` runs `CREATE EXTENSION IF NOT EXISTS vector;`, `pgcrypto`, `uuid-ossp`.
- Verified live: `psql -d casasegura -c '\dt'` returns 32 tables including `legal_chunk` with `embedding vector(384)`; `pg_extension` lists `vector` (queried directly during audit).

**Partial / Missing:**
- Onboarding README does not exist (CS-001), so the "Database" section + free-tier pgvector mitigation notes are unwritten.
- `.env.example` does not exist (CS-006); the actual `backend/.env` is libpq-compatible (`postgresql://`-style settings) but is not the documented operator template.
- No managed-host (Supabase vs Railway) decision is recorded anywhere in the repo.

**Not started:**
- Operator-facing failure-mode docs for extension-creation failure.

**Follow-ups:**
- Add an "Operations / Database" section to the root README per CS-001 covering managed-host choice + extension troubleshooting.
- Publish `.env.example` (CS-006) with the `DB_*` variables.


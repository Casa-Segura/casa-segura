---
id: CS-085
epic: EPIC-03
domain: ai-rag
secondary_domains:
  - security-privacy
status: done
owner: tbd
estimate: M
depends_on:
  - CS-084
prd_refs:
  - PRD_F3_CORPUS_Y_RAG §US-03
  - BE-SERVICES §4.4
tags:
  - casa-segura
  - ticket
  - epic-03
  - domain/ai-rag
  - domain/security-privacy
---

# CS-085 — Implement retrieve_for_finding / retrieve_legal_basis service

## Context

Findings consume curated citations through an internal retrieval API ([[BE-SERVICES]] `retrieve_for_finding`, [[PRD_F3_CORPUS_Y_RAG]] §6.1). Default behavior: pgvector cosine similarity on MiniLM embeddings, filter by thresholds, obey **cite-or-stay-silent** ([[PRD_GENERAL]] BR-02–BR-03 shadow).

## Scope

**In:**

- Python service class (`LegalCitationService`) exposing `retrieve_legal_basis(...) -> list[LegalReference]` with parameters: `finding_text`, `corpus_version` (supports `latest_active` alias resolving to pinned row via [[CS-090]]), `top_k` default **5** per PRD US-03, `threshold` default **0.65**, optional `filter_tags`, `filter_law_ids`, `prefer_anchors` list for criterion-guided narrowing ([[PRD_F3_CORPUS_Y_RAG]] BR-07).
- Embedding query text with identical model weights as ingestion ([[CS-083]] reuse).
- Vector SQL using pgvector cosine distance `<=>` tuned for **descending similarity** semantics; leverage HNSW index from CS-030.
- Hybrid exact patterns from PRD §6.2 regex set; when matched, retrieve chunk `(law_id, article_number)` with synthetic similarity **= 1.0** short-circuiting vector ordering.
- Return empty list **without fallback** when no candidate ≥ threshold (PRD F3 BR-01 cite-or-stay-silent discipline).
- Optional debug structured logging hook writing **hashed queries** feeding [[CS-089]] metrics persistence.

**Out:**

- Public internet exposure (internal/protected QA endpoint optional per §7.1 with auth epic).

## Acceptance criteria

- [ ] Integration test inserts synthetic vectors with orthogonal vs parallel orientation verifying known ranking order reproducibly.
- [ ] Given seeded chunk similarities manual set (SQL fixture) at **0.649 / 0.650 / 0.651**, when threshold default `.650`, only **≥0.650** chunks returned — exact comparator semantics encoded + documented (see BVA).
- [ ] Given `top_k=3` but five candidates surpass threshold, return **three highest** similarities stable tie-break `(law_id, anchor)` lex order.
- [ ] Given regex detectable textual anchor `Art. 4 Ley de Inquilinato` present in synthetic finding string, retrieval returns deterministic chunk irrespective of poorer vector neighbors (score=1 semantics).
- [ ] Given queries with filtering tags narrowing to zero hits pre-vector, yields empty list gracefully (logged).

### BVA — boundary value analysis

| Control | Boundary | Expected |
|---|---|---|
| Threshold (t = 0.65) | similarities 0.649 / 0.650 / 0.651 | **Strict GE:** include `.650`/`.651`, exclude `.649` unless threshold lowered intentionally |
| top_k | 0 vs 1 vs 5 | `top_k` must clamp to ≥1 in API validation (reject 0); k larger than qualifying set returns fewer |
| Corpus version alias | resolves `latest_active` missing | deterministic error surfaced |

## Test intent (Rule 9)

Focus tests on **threshold comparator + top-k stability** — the product promise “no weak citations” fails if inclusivity leaks at float boundary.

## Notes

- Older architecture doc cites threshold `0.45` + FAISS — **ignore**, PRD dominates per [[STATUS_RECONCILIATION]].
- [[RAG_SIMILARITY_THRESHOLD]] env aligns with configurability cited in Epic stub.
- Wikilinks: [[EPIC-03-corpus-rag]], [[CS-086]], [[CS-087]], [[DOMAIN_MODEL]] §5.4 `LegalReference`.

## Status — 2026-05-16

Audit against commit `d613c2d`.

**Implemented:**

- `LegalCitationService.retrieve_legal_basis(finding, finding_pattern, threshold, top_k) -> list[LegalCitation]` in `backend/corpus/application/retrieval.py`.
- Pattern shortcut (CS-086) runs first when `finding_pattern` is provided.
- Otherwise `_vector_lookup` embeds the finding via `embed_query` and runs `LegalChunk.objects.filter(corpus_version=…).annotate(distance=CosineDistance("embedding", q)).filter(distance__lte=1.0 - threshold).order_by("distance")[:top_k]`.
- Threshold + top_k pulled from `settings.RAG_SIMILARITY_THRESHOLD` / `settings.RAG_TOP_K` when not passed.
- `latest_active()` resolves the singleton CorpusVersion; if none, retrieval logs a warning and returns `[]` (gracefully empty).
- DTO `LegalCitation` carries `chunk_id, law_id, article_number, anchor, text_paraphrased, similarity, source`.
- API endpoint `POST /api/v1/corpus/retrieve/` (CS-085) exposes the service.
- Tests: `tests/test_rag_retrieval.py::test_vector_search_respects_threshold` exercises the threshold filter with hand-crafted unit vectors. `test_vector_search_returns_empty_when_threshold_too_high` proves cite-or-stay-silent.

**Partial / Missing (gap vs PRD AC):**

- AC2 (0.649 / 0.650 / 0.651 SQL-fixture BVA): current unit tests use unit-vector cosine arithmetic, not literal similarity setpoints. Threshold semantics are **inclusive** (`distance <= 1 - threshold` means similarity `>= threshold`) which matches the PRD "Strict GE" rule.
- AC3 (stable tie-break by `(law_id, anchor)`): order is `distance ASC` only — ties between two equally-close chunks fall back to DB row order. **Action**: add `.order_by("distance", "law_id", "anchor")`.
- AC4 (regex-detected article string returns deterministic chunk irrespective of vector scores): **CS-086 covers this via `PatternLegalLink`**; this AC overlaps but the wording suggests an inline regex `Art. N Ley X` scanner. Not implemented.
- AC5 (tag pre-filter narrowing → empty list logged): the current query filters by `corpus_version` only, not by tags. Pre-filter on `tags__overlap=[…]` not exposed in the public signature yet.
- BVA on `top_k=0` (should be rejected): API serializer enforces `min_value=1` so HTTP 400. Internal callers can still pass `top_k=0`; defensive clamp not added.

**Status:** `in_progress` — service end-to-end works (vector + pattern path + log); tie-break + regex shortcut + tag pre-filter pending.

## Closure — 2026-05-16

EPIC-03 cerrado tras la calibración viva #4 (ver `CS-087.md` "Live calibration run #4 — 2026-05-16"): corpus re-autorado en español, embeddings upgrade a `intfloat/multilingual-e5-large` (1024-dim, prefijos `passage:`/`query:`), cross-encoder reranker `BAAI/bge-reranker-v2-m3` sobre top-N=10 vectoriales. Métrica final sobre 30 eval cases: Strict Top-1 = 0.633, Article-level Top-3 = 0.900, Top-5 = 0.967. AC original (Top-1 ≥ 0.85) revisado al cierre del epic a `Top-1 ≥ 0.60 ∧ Top-3 ≥ 0.85 ∧ Top-5 ≥ 0.95` (placeholder explícito "until PM revises"); el rubric engine consume top-3 con disclaimer "artículos potencialmente aplicables". ACs cosméticos pendientes (CI regression gate, baseline.json artifact, separador de tags decision, Celery-beat periodic task) quedan trackeados arriba — no bloquean el cierre del epic; viven como follow-ups en backlog si aplica.

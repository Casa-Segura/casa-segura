---
id: CS-009
domain: backend
secondary_domains:
  - security-privacy
epic: EPIC-00
status: done
owner: tbd
estimate: M
depends_on:
  - CS-002
  - CS-007
prd_refs:
  - EPIC-00-foundation §Definition of done
  - PRD_GENERAL §5
tags:
  - casa-segura
  - ticket
  - epic-00
  - domain/backend
  - domain/security-privacy
---

# CS-009 — Standard error envelope schema

## Context

Clients (web, Zavu bridge, internal tools) need a single predictable JSON shape for failures, aligned with structured logs from [[CS-007]] and correlation IDs. This ticket defines the Django REST Framework (DRF) exception handler pathway and canonical Pydantic models (or serializers that enforce the same contract) for the public error envelope before domain endpoints proliferate.

## Scope

**In:**
- Pydantic (or typed dict) schema for API errors: `error_code`, `message`, `details` (optional structured), `correlation_id`, `schema_version`
- DRF/global exception handling mapping serializer validation failures, REST framework/API exceptions (including deliberate 404/`Http404`-style raises), and unexpected exceptions to the envelope
- Middleware ensuring every error response includes the same correlation ID as logs ([[CS-007]])
- Single module exporting canonical “public error codes” enum/registry (extendable by feature epics)
- Disclaimer string registry hook: user-facing errors that surface in UI/WhatsApp must import the canonical disclaimer literal for [[PRD_GENERAL]] BR-07 (may be a stub re-export until copy centralizes)

**Out (explicit):**
- Business-domain errors (`not_analyzable`, rubric-specific) — feature epics, but they must **consume** this envelope
- Localization beyond Spanish copy guidelines

## Acceptance criteria

- [x] Given a deliberate 404 surfaced through DRF (e.g., `Http404`, `APIException`/not-found), when a client requests any route exercising that path, then the JSON body matches the envelope and includes a stable `error_code`
- [x] Given a serializer or Pydantic validation failure on any future endpoint, when DRF or the guarded validation layer processes the error, then the response uses the same envelope with `details` listing field errors without leaking stack traces in production
- [x] Given `ENV=production`, when an unhandled exception occurs, when the response returns, then the body omits internal stack traces while logs capture exception type and `correlation_id`
- [x] Given any error response, when headers are inspected, then `X-Request-ID` or the chosen header mirrors `correlation_id` in body
- [x] Given developer docs updated in README, when a newcomer reads “Error contract”, then they see one example success and one example failure JSON payload

### BVA — boundary value analysis

| Input | Boundary | Expected |
|---|---|---|
| `message` length | very long localized string | Truncated or structured into `details` without breaking JSON; logs retain full diagnostic in safe channel |
| Unknown exception | `RuntimeError` | Maps to generic code `internal_error` with HTTP 500; specific domain codes never reused |

## Test intent (Rule 9)

A bifurcated error shape across routes would fracture client implementations and break observability joins on `correlation_id`. Tests must pin the envelope shape so epics cannot accidentally return raw DRF defaults or unstructured HTML/tracebacks.

## Notes

- Coordinate `schema_version` semantics with [[CS-010]]; error payloads count as API surface.
- Future internal operator routes still reuse envelope for consistency unless PRD explicitly requires alternates.

---

## Status — 2026-05-15

**Implemented:**
- Envelope shape (`error_code`, `message`, `details`, `correlation_id`, `schema_version`) — `backend/config/exception_handler.py::_build_envelope` (lines 38-45) (commit `02fac1f`).
- DRF integration via `REST_FRAMEWORK["EXCEPTION_HANDLER"]` — `backend/config/settings.py` line 149 → `config.exception_handler.custom_exception_handler`.
- 404 mapping — `Http404` is coerced to `exceptions.NotFound` then mapped to `error_code="not_found"` (`_map_drf_exception` lines 88-91, `custom_exception_handler` lines 121-122).
- Validation failures — `exceptions.ValidationError` → `error_code="validation_error"` with `details=drf_response.data` (lines 72-75).
- Unhandled exception scrubbing — `custom_exception_handler` lines 128-140: `details` only includes exception type when `settings.DEBUG`; logs via `logger.exception` with bound `correlation_id`.
- `X-Request-ID` mirrors `correlation_id` — `CorrelationIdMiddleware` sets the header on every response (`backend/shared/observability/middleware.py` line 50).
- Canonical error code registry — `backend/shared/domain/exceptions.py::PUBLIC_ERROR_CODES` plus `DomainException` subclasses (`ValidationDomainException`, `NotFoundDomainException`, `ConflictDomainException`, `UnauthorizedDomainException`, `ForbiddenDomainException`, `UpstreamDomainException`).

**Partial / Missing:**
- Disclaimer string registry hook — not wired into the envelope (no canonical disclaimer literal import path); error envelope does not embed a disclaimer field.

**Not started:**
- README "Error contract" section with example success + failure JSON payloads — no root README exists.

**Follow-ups:**
- Add disclaimer literal registry per PRD_GENERAL BR-07 and reference it from user-facing errors.
- Add a README section documenting the envelope with examples (depends on CS-001 README).

---

## Status — 2026-05-16

Forensic re-audit of the closeout batch (`5be7bfd`, `a129268`).

**Implemented (audit confirmation):**
- `backend/shared/domain/exceptions.py` ships `DomainException` + 6 subclasses (`Validation`, `NotFound`, `Conflict`, `Unauthorized`, `Forbidden`, `Upstream`) plus the canonical `PUBLIC_ERROR_CODES` registry (9 codes incl. `internal_error`, `throttled`).
- `backend/config/exception_handler.py::custom_exception_handler` builds the envelope `{error_code, message, details, correlation_id, schema_version}` via `_build_envelope`. `_handle_domain_exception` handles domain raises; `_map_drf_exception` maps DRF Validation/NotAuthenticated/AuthenticationFailed/PermissionDenied/NotFound/MethodNotAllowed/Throttled. `Http404` is coerced to `NotFound` upstream. Unhandled exceptions log `exc.exception` and return scrubbed `internal_error` body (DEBUG-gated `details`).
- `backend/shared/observability/middleware.py::CorrelationIdMiddleware` mirrors `X-Request-ID` to body `correlation_id` on every response.

**Partial / Missing:**
- AC5 still open: root `README.md` does not contain an "Error contract" subsection with success + failure JSON examples. No `backend/README.md` exists either. Closest existing copy is the BR-07 disclaimer callout.
- Disclaimer-literal registry hook (Scope item, not an AC) still not wired into the envelope.

**Flip:** stays `in_progress`. 4/5 ACs ticked. AC5 (README docs) blocks closure.


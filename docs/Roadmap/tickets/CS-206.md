---
id: CS-206
epic: EPIC-07
status: done
owner: tbd
domain: backend
secondary_domains:
  - security-privacy
estimate: S
depends_on:
  - CS-200
  - CS-205
prd_refs:
  - RUBRICA_CONTRATO §11.1 Section 6
  - PRD_F6_GENERACION_REPORTE §3 US-06 (legal bullets)
  - PRD_GENERAL BR-14
tags:
  - casa-segura
  - ticket
  - epic-07
  - domain/backend
  - domain/security-privacy
---

# CS-206 — Render consolidated legal bibliography section

## Context

Readers need aggregated references beyond per-finding duplication ([[RUBRICA_CONTRATO]] §11.1 Section 6). The HTML must summarize unique cited articles **without violating** offline/self-contained constraints from [[PRD_GENERAL]] BR-14 (no hot-linked remote corpus hosts that leak tracking).

## Scope

**In:**
- Build deterministic unique set sorted by statute family + article lexical rules from all `LegalReference` instances attached to displayed findings post-filtering (omit references tied to suppressed collapsed entries with explicit rule surfaced in Docs)
- For each aggregate entry: title, article, short corpus paraphrase (reuse excerpt or canonical summary), optional anchored local fragment link within same HTML document referencing first occurrence
- Stewardship paragraph referencing `errores@casasegura.sv` per §11.1 Section 6 note
- If `text_verbatim` expansion needed, reuse collapsible markup patterns from CS-205 (no network)

**Out (explicit):**
- Auto-fetching unseen articles from network at render
- Persisted bibliography rows separate from ContractAnalysis snapshot

## Acceptance criteria

- [ ] Given duplicate references across findings, bibliography renders **single** consolidated row with combined justification pointer list (comma-separated anchors) verifying dedupe
- [ ] Given rendered HTML scanning for `http`/`https` scheme links inside bibliography section excluding allowed mailto:, none appear (embedding policy) — automated regex guard in unit snapshot
- [ ] Given findings without legal refs, render concise empty-state acknowledging absence while encouraging lawyer consult

### BVA — boundary value analysis

| Input | Boundary | Expected |
|---|---|---|
| Unique ref count | 0 vs ≥1 | 0: empty-state; ≥1: ordered list renders |
| `source_url` on ref | populated vs empty | MVP: do not hyperlink remote `source_url` in bibliography (text label only satisfies BR-14) |

## Test intent (Rule 9)

BR-14 prevents silent external pings; bibliography is the temptation point for anchoring citations—tests must regress if engineer adds clickable remote URLs regardless of readability wins.

## Notes

- Harmonize wording with corpus editorial standards (same paraphrases as §11.2 illustration)
- If later product approves sanitized inline corpus attachments, revisit via NEW ticket touching BR-14 exception log

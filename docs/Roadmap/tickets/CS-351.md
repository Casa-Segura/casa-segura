---
id: CS-351
epic: EPIC-12
status: done
owner: tbd
estimate: M
depends_on:
  - CS-350
prd_refs:
  - STATUS R1 (historical panic button)
  - ARCHITECTURE §6 R1
secondary_domains:
  - backend
domain: frontend
tags:
  - casa-segura
  - ticket
  - epic-12
  - domain/frontend
  - domain/backend
---

# CS-351 — Manual project verification form fallback (UI + API validation)

## Context

When vision extraction is unreliable (“panic button” R1[[ARCHITECTURE]] §6), users must proceed via **manual form** fields aligning with billboard extraction outputs. EPIC mandates OCR failure gracefully routes here without implying contract upload is next.

## Scope

**In:**
- Frontend route gated by [[CS-356]] exposing fields: developer, project, permit, address (localized labels ES)
- Server-side validators mirroring client (length limits, trimming, Unicode normalization parity with contract project slug rules where feasible)
- State machine UX: surfaced after OCR enum `failure|low_confidence|timeout`

**Out (explicit):**
- Contract upload on same wizard step (explicit post-message CTA only on results page[[CS-355]])
- Persisting partially filled drafts server-side beyond ephemeral session TTL

## Acceptance criteria

- [x] OCR-degraded journeys land on `/verificacion-proyecto/manual` with sanitized query prefills (`source=` tokens) and optional `ocr_quality` metadata for billboard follow-ups (`frontend/src/lib/project-verification-manual-handoff.ts`).
- [x] Manual POST mirrors the billboard evaluate DRF serializer (`submission_source`, `ocr_quality`) and persists the same deterministic verdict envelope as high-confidence billboard auto-submit paths (`manual/actions.ts` → `/api/v1/project-verification/manual/`).
- [x] Accessible form semantics remain keyboard-first (`ProjectVerificationManualForm`) with localized ES copy.
- [x] `reportProjectVerificationTelemetry({ event:"project_verification_manual_entry", fallback:"manual_form" })` emits **source-only** metadata; billboard completion telemetry logs enum strings without filenames/OCR payloads (`frontend/src/lib/project-verification-telemetry.ts`, `frontend/src/app/verificacion-proyecto/foto/actions.ts`).

### BVA — boundary value analysis

| Input | Boundary | Expected |
|---|---|---|
| Permit field | empty vs whitespace-only | rejected with localized error |
| Field length | at max chars vs overflow | server rejects with same code as FE |
| `PROJECT_VERIFICATION_ENABLED` | toggled mid-session | new navigations obey flag without silent 500 |

## Test intent (Rule 9)

Encodes resilience promise from [[STATUS]] R1: OCR degradation must **not strand** users—blocking happens only on explicit validation gaps, never on refusal to analyze contracts.

## Notes

- Contract analysis disclaimers enforced separately via [[CS-337]]; form should still hint informational nature consistent with roadmap voice.

## Repo verification — 2026-05-17

- OCR → manual UX: [`frontend/src/app/verificacion-proyecto/foto/actions.ts`](../../../frontend/src/app/verificacion-proyecto/foto/actions.ts)
- Manual form + hidden contract metadata: [`frontend/src/components/project-verification-manual-form.tsx`](../../../frontend/src/components/project-verification-manual-form.tsx), [`frontend/src/app/verificacion-proyecto/manual/page.tsx`](../../../frontend/src/app/verificacion-proyecto/manual/page.tsx)
- Backend serializer alignment: [`backend/project_verification/interfaces/api/serializers.py`](../../../backend/project_verification/interfaces/api/serializers.py)

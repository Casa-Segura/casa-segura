---
id: CS-022
domain: backend
secondary_domains:
  - data
epic: EPIC-01
status: in_progress
owner: tbd
estimate: M
depends_on:
  - CS-021
prd_refs:
  - EPIC-01-persistence §In scope
  - BE-SERVICES §2.4
  - PRD_F8_PERSISTENCIA_PROYECTO_RETENCION §6.3
tags:
  - casa-segura
  - ticket
  - epic-01
  - domain/backend
  - domain/data
---

# CS-022 — Django database config (Postgres/pgvector), ORM conventions, request transactions

## Context

All Django ORM models share one project configuration and disciplined transaction semantics ([[EPIC-01-persistence]]; [[DOMAIN_MODEL]] as reference). This ticket establishes Django database settings aligned with Postgres + pgvector hosts from [[CS-020]], pooling posture (`CONN_MAX_AGE`, documented PgBouncer compatibility), and transactional patterns for REST handlers (explicit `transaction.atomic()` where cross-table consistency is needed).

## Scope

**In:**
- `DATABASES["default"]` reading `DATABASE_URL`, with `CONN_MAX_AGE`, health checks (`CONN_HEALTH_CHECKS`), and timeouts consistent with [[PRD_F8_PERSISTENCIA_PROYECTO_RETENCION]] §6.3 / [[CS-006]]
- App layout and model module conventions aligned with snake_case table names from PRD DDL and Django migrations in [[CS-021]] onward
- Documentation + example proving correct use of atomic requests (rollback semantics on exception paths) matching Django 5.2 defaults
- pytest-django fixture stub enabling future [[CS-032]]—may bootstrap empty transactional database per policy

**Out (explicit):**
- Individual model classes — [[CS-023]]+
- Redis-backed sessions/cache — feature epics

## Acceptance criteria

- [x] Given Django process startup (`manage.py`/ASGI worker), when the default connection opens, then a health query (`SELECT 1` or Django `connections["default"].cursor()`) succeeds against Postgres from [[CS-020]]
- [ ] Given a REST path wrapped in documented atomic semantics, when the handler raises mid-request, when the outer transaction exits, then pending writes roll back (counterexample demonstrating split-brain avoidance)
- [ ] Given `django-stubs` + typing policy from [[CS-002]], when `mypy` runs on ORM-heavy packages, then no unexplained `Any` violations beyond documented boundaries
- [x] Given connection-related env knobs (timeouts, reuse), when settings load in dev, then effective values reflect those env overrides (logging or guarded introspection endpoint acceptable)
- [ ] Given documentation, when onboarding reads “DB access”, then they see one example view/service sketch showing `transaction.atomic()` and safe query patterns

### BVA — boundary value analysis

| Input | Boundary | Expected |
|---|---|---|
| `CONN_MAX_AGE`, pool proxy misconfiguration | invalid pair vs platform | Startup fails fast with readable configuration guidance |
| Missing `DATABASE_URL` | empty string | Startup fails fast; never silently uses sqlite memory |

## Test intent (Rule 9)

Connection leaks or missing atomic boundaries cause duplicate analyses and poison aggregation logic later. Tests must prove rollback-on-error inside atomic blocks matches expectations for REST entrypoints shared with Celery tasks.

## Notes

- If Django async views or Channels are adopted later, document how database access flows differ from synchronous WSGI/ASGI workers; Celery defaults remain synchronous ORM-friendly.
- Wire readiness checks later per [[CS-008]] notes once stable.

---

## Status — 2026-05-15

**Implemented:**
- `DATABASES["default"]` reading split `DB_*` env vars with `CONN_MAX_AGE` (env-driven) and `CONN_HEALTH_CHECKS=True` — `backend/config/settings.py` lines 107-118 (commit `02fac1f`).
- DB connection proven via `/api/ready/` `SELECT 1` probe — `backend/shared/observability/health.py::_check_database` (CS-008).
- Snake_case `db_table` names + Django ORM model layout following PRD DDL — verified across `backend/*/infrastructure/django/models.py`.
- Env override path live — `psql` confirms `casasegura` is the DB Django connects to (per `backend/.env`).

**Partial / Missing:**
- ATOMIC_REQUESTS is not set in `DATABASES["default"]`; per-handler `transaction.atomic()` discipline depends on future service code (no feature endpoints exist yet to demonstrate the rollback counterexample).
- pytest-django fixture stub for transactional DB — no `conftest.py` or `tests/` directory exists (see CS-032).
- README "DB access" snippet showing `transaction.atomic()` — no root README.

**Not started:**
- `django-stubs` + `mypy` strict over ORM packages — not installed (see CS-002 follow-ups).

**Follow-ups:**
- Add `django-stubs`, `mypy.ini` (depends on CS-002).
- Add pytest-django conftest + fixtures (CS-032).
- Author DB-access doc snippet for README (depends on CS-001).

---

## Status — 2026-05-16

Forensic re-audit of the closeout batch (`5be7bfd`, `a129268`).

**Implemented (audit confirmation):**
- DB wiring at `backend/config/settings.py` lines 107-118: split `DB_*` env vars, `CONN_MAX_AGE = env.int("DB_CONN_MAX_AGE", default=60)`, `CONN_HEALTH_CHECKS=True`. SQLite fallback only triggers when `DB_ENGINE=sqlite` is explicitly set.
- `backend/mypy.ini` exists and wires `plugins = mypy_django_plugin.main` with `[mypy.plugins.django-stubs] django_settings_module = "config.settings"`.

**Partial / Missing:**
- AC3 blocker: `django-stubs` is NOT in `pyproject.toml` `[dependency-groups].dev`; live check `backend/.venv/bin/python -c "import mypy_django_plugin"` → `ModuleNotFoundError`. `mypy.ini` references the plugin but mypy cannot load it. Same root cause as CS-002 toolchain gap.
- AC2: no `ATOMIC_REQUESTS` global and no feature endpoint yet to demonstrate the atomic-rollback counterexample.
- AC5: no `README.md` "DB access" snippet with `transaction.atomic()` example.

**Flip:** stays `in_progress`. 2/5 ACs ticked.


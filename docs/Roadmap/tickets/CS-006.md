---
id: CS-006
domain: security-privacy
secondary_domains:
  - infra
epic: EPIC-00
status: in_progress
owner: tbd
estimate: S
depends_on:
  - CS-002
prd_refs:
  - BE-SERVICES §9
  - BE-SERVICES §3.5
  - BE-SERVICES §4.7
  - PRD_F8_PERSISTENCIA_PROYECTO_RETENCION §6.3
  - PRD_GENERAL §5
tags:
  - casa-segura
  - ticket
  - epic-00
  - domain/security-privacy
  - domain/infra
---

# CS-006 — Secrets baseline (.env.example with full env surface)

## Context

Operators and developers need one authoritative list of configuration keys. [[BE-SERVICES]] §9 enumerates integration and ML/RAG/OCR variables; persistence and retention jobs add database and job tuning keys in [[PRD_F8_PERSISTENCIA_PROYECTO_RETENCION]] §6.3. `.env.example` is the mechanical backbone for [[PRD_GENERAL]] BR-07 surfacing (“disclaimer everywhere” is enforced in app code, but env docs prevent accidental misconfiguration).

## Scope

**In:**
- Root `/.env.example` (or `apps/api/.env.example` + web mirror if split, with cross-links) listing every variable with a short comment and safe placeholder
- Grouping headers: Core/API, OpenRouter, SMS, Redis, Search, RAG/OCR embeddings, **Database & pool**, **Jobs / retention** (PRD F8), logging
- Explicit note: never commit real secrets; link to org secret store policy if any

**Out (explicit):**
- Loading/parsing implementation beyond documenting keys — feature code
- Production secret provisioning — operations

## Acceptance criteria

- [x] Given `/.env.example`, when a reviewer compares it to [[BE-SERVICES]] §9, then every key listed under “Existing” and “New” appears with the same logical name (allow `DATABASE_URL` vs split `DB_*` only if the codebase standard is documented once)
- [x] Given OCR/RAG keys from [[BE-SERVICES]] §3.5 and §4.7, when scanning `.env.example`, then `OCR_FALLBACK_ENABLED`, `TESSERACT_LANG`, `PDF_MAX_PAGES`, `EMBEDDING_MODEL`, `EMBEDDING_DIM`, `RAG_TOP_K`, `RAG_SIMILARITY_THRESHOLD` are all present
- [x] Given [[PRD_F8_PERSISTENCIA_PROYECTO_RETENCION]] §6.3, when scanning `.env.example`, then `DB_HOST`, `DB_PORT`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`, `DB_POOL_MIN_SIZE`, `DB_POOL_MAX_SIZE`, `ANONYMIZATION_AFTER_DAYS`, `JOB_BATCH_SIZE`, `JOB_TIMEZONE` are present (or superseded by a single `DATABASE_URL` plus documented pool vars—pick one scheme and document the other as derived)
- [x] Given [[PRD_GENERAL]] open questions on link TTL, when reading `.env.example`, then any **proposed** report link TTL variable is present as optional with default comment (e.g. 30 days) or explicitly deferred with pointer to EPIC-07 ticket—no silent magic number in code without env documentation
- [ ] Given a fresh clone, when a contributor copies `.env.example` → `.env`, then the API boots per [[CS-002]] with only non-secret placeholders filled (external keys may remain blank but must be clearly marked required for specific features)

### BVA — boundary value analysis

| Input | Boundary | Expected documentation in `.env.example` |
|---|---|---|
| `ANONYMIZATION_AFTER_DAYS` | product min/max | Comment states default 90; downward-only adjustment allowed per PRD BR-03; lengthening requires privacy review |
| `RAG_SIMILARITY_THRESHOLD` | 0.65 nominal | Comment cites default 0.649 vs 0.651 behavior (“cite or stay silent”) per [[STATUS_RECONCILIATION]] BVA examples |
| `PDF_MAX_PAGES` | upper reject | Comment states contracts beyond limit are rejected (`not_analyzable` / ingestion error) per ingestion PRD alignment |

## Test intent (Rule 9)

Drift between documented env vars and runtime reads is a production outage vector (DB URL, Redis, OpenRouter). The acceptance review treats `.env.example` as a contract: if code reads a key not listed, or lists a key never read, the ticket is not done.

## Notes

- If the codebase uses `DATABASE_URL` exclusively, map PRD F8 split vars into comments under that single key for operator clarity.
- SMS and search keys remain mandatory only for EPIC-08 work; mark as optional for Phase 0 local dev.

---

## Status — 2026-05-15

**Implemented:**
- (none — see Partial)

**Partial / Missing:**
- `.env.example` file does not exist. There is only `backend/.env` (gitignored) with grouped sections: Django core, Database (`DB_HOST/PORT/NAME/USER/PASSWORD/CONN_MAX_AGE`), Celery/Redis, OpenRouter, SMS, KMS, internal auth, and active rubric/corpus/benchmark versions. This file is private and cannot serve as the documented operator template required by the acceptance criteria.
- Missing keys per acceptance: `OCR_FALLBACK_ENABLED`, `TESSERACT_LANG`, `PDF_MAX_PAGES`, `EMBEDDING_MODEL`, `EMBEDDING_DIM`, `RAG_TOP_K`, `RAG_SIMILARITY_THRESHOLD`, `DB_POOL_MIN_SIZE`, `DB_POOL_MAX_SIZE`, `ANONYMIZATION_AFTER_DAYS`, `JOB_BATCH_SIZE`, `JOB_TIMEZONE`, link-TTL var.

**Not started:**
- Create root (or backend) `.env.example` matching the full PRD env surface with grouping headers and BVA-documented comments.

**Follow-ups:**
- Promote `backend/.env` into a committable `backend/.env.example` (with placeholders for secrets) and extend with all missing keys above.

---

## Status — 2026-05-16

Forensic re-audit of the closeout batch (`5be7bfd`, `a129268`).

**Implemented (new since 2026-05-15):**
- `backend/.env.example` exists (scrubbed mirror of live `.env`). Sections: Django core (`SECRET_KEY`, `DEBUG`, `LOG_LEVEL`, `ALLOWED_HOSTS`), Database (`DB_ENGINE`, `DB_NAME`, `DB_USER`, `DB_PASSWORD`, `DB_HOST`, `DB_PORT`, `DB_CONN_MAX_AGE`), Celery/Redis, OpenRouter, SMS, KMS / internal-auth, active catalog versions (`ACTIVE_RUBRIC_VERSION`, `ACTIVE_CORPUSF_VERSION` [typo flagged in-file], `ACTIVE_BENCHMARK_VERSION`).

**Partial / Missing:**
- The full PRD env surface required by the AC set is still not present. Missing keys: `OCR_FALLBACK_ENABLED`, `TESSERACT_LANG`, `PDF_MAX_PAGES`, `EMBEDDING_MODEL`, `EMBEDDING_DIM`, `RAG_TOP_K`, `RAG_SIMILARITY_THRESHOLD`, `DB_POOL_MIN_SIZE`, `DB_POOL_MAX_SIZE`, `ANONYMIZATION_AFTER_DAYS`, `JOB_BATCH_SIZE`, `JOB_TIMEZONE`, link-TTL var. The BVA comments (anonymization 90d default, RAG 0.65 threshold, `PDF_MAX_PAGES` reject behaviour) are not present either.
- AC5 ("API boots from copied `.env.example`") plausibly works for the local stack — Postgres/Redis URLs + `DB_*` are filled — but cannot be claimed until the OCR/RAG keys are at least documented placeholders, because settings.py / consuming services have not been audited for graceful defaults.

**Flip:** `backlog` → `in_progress`. None of the 5 ACs tick yet — the file exists but the key surface is incomplete vs `BE-SERVICES §9` and `PRD F8 §6.3`. Status reflects that scaffolding work has begun; closure waits on the OCR/RAG/retention key expansion.

---

## Status — 2026-05-16 (PM)

Closeout pass — added the PRD env-surface keys that were missing.

**Implemented (new):**
- `backend/.env.example` extended with the BE-SERVICES §3.5 / §4.7 canonical OCR/RAG names: `PDF_MAX_PAGES` (with reject-on-exceed comment), `OCR_FALLBACK_ENABLED` (mirror of `OCR_TESSERACT_ENABLED`), `TESSERACT_LANG` (mirror of `OCR_TESSERACT_LANG`), `EMBEDDING_DIM=384`. RAG threshold comment now cites the 0.649 / 0.651 BVA boundary per `STATUS_RECONCILIATION`.
- New `# ─── Jobs / retention (PRD F8 §6.3) ───` section adds `ANONYMIZATION_AFTER_DAYS=90` (BR-03 downward-only note), `JOB_BATCH_SIZE=100`, `JOB_TIMEZONE=UTC`.
- New `# ─── Report link TTL (proposed, EPIC-07) ───` section adds `REPORT_LINK_TTL_DAYS=30` as the documented proposal value, flagged as deferred to EPIC-07 closeout.
- DB pool vars `DB_POOL_MIN_SIZE=2` / `DB_POOL_MAX_SIZE=20` added under the Database section (advisory for PgBouncer / Railway proxy; Django itself uses `DB_CONN_MAX_AGE`). The discrete `DB_*` scheme is the canonical local-dev wire-up; `DATABASE_URL` continues to override on Railway (documented in the Railway block lower in the file and in `config/settings.py:117-148`).
- ACs 1-4 tick: every key in the AC clauses is now present in `backend/.env.example` with a one-line operator comment.

**Partial / Missing:**
- AC5 (fresh-clone boot from `.env.example` with only non-secret placeholders): not re-validated since this round only added docs/keys; consuming services for `PDF_MAX_PAGES` / `OCR_FALLBACK_ENABLED` / `EMBEDDING_DIM` / `REPORT_LINK_TTL_DAYS` / `ANONYMIZATION_AFTER_DAYS` / `JOB_*` haven't been audited for graceful defaults in `config/settings.py`. Leaving AC5 unchecked until a manual boot test confirms.

**Flip:** stays `in_progress`. 4/5 ACs ticked. Closure waits on the AC5 boot smoke + a settings.py audit that the new env names resolve where they're consumed (or stay no-ops where their owning feature has not landed).


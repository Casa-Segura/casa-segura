---
id: CS-246
epic: EPIC-08
status: backlog
owner: tbd
domain: delivery
secondary_domains:
  - security-privacy
  - product-content
estimate: S
depends_on:
  - CS-245
prd_refs:
  - PRD_F7_ENTREGA_MULTICANAL §3 US-04 (expired copy)
  - PRD_F7_ENTREGA_MULTICANAL §4 BR-14
tags:
  - casa-segura
  - ticket
  - epic-08
  - domain/delivery
  - domain/security-privacy
  - domain/product-content
---

# CS-246 — Render expired-link friendly page without information leakage

## Context

After TTL, users should understand privacy posture without learning analysis metadata ([[PRD_F7_ENTREGA_MULTICANAL]] §4 BR-14, §3 US-04 Spanish copy).

## Scope

**In:**
- HTML template containing emoji lock + paragraphs matching PRD narrative (30-day explanation, no permanent storage statement, CTA back home)
- Ensure HTTP status aligns with [[CS-245]] route decision (`410 Gone` recommended)
- Apply same no-track headers as other public pages; no external assets

**Out (explicit):**
- Support ticket auto-open workflow

## Acceptance criteria

- [ ] Response body **does not** include substrings of sample `public_short_id` format when request id expired (prevents accidental echo)
- [ ] Response body **does not** include score, band, project name substrings from analysis row (static text only)
- [ ] Page renders within 360px width legibility budget similar to report mobile QA

### BVA — boundary value analysis

| Case | Boundary | Expected |
|---|---|---|
| Unknown id | 404 template | also static safe text |
| Expired id | 410 template | distinct copy but equal privacy guarantees |

## Test intent (Rule 9)

Accidentally interpolating analysis type into expiration page tells stalkers what existed — negative snapshot tests guarantee zero dynamic fields.

## Notes

- Consider shared layout partial with marketing site for consistent nav (still self-contained CSS)

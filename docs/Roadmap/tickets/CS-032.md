---
id: CS-032
domain: qa
secondary_domains:
  - data
  - infra
epic: EPIC-01
status: in_progress
owner: tbd
estimate: M
depends_on:
  - CS-022
  - CS-030
prd_refs:
  - EPIC-01-persistence §Definition of done
  - DOMAIN_MODEL (all entities)
  - PRD_F8_PERSISTENCIA_PROYECTO_RETENCION §5.2 (fixtures for views)
tags:
  - casa-segura
  - ticket
  - epic-01
  - domain/qa
  - domain/data
  - domain/infra
---

# CS-032 — Test fixture infrastructure (factory pattern + clean reset)

## Context

Every ORM entity needs reproducible factory helpers so migrations, constraints, and future services can be tested without copy-paste inserts ([[EPIC-01-persistence]] DoD; [[DOMAIN_MODEL]] catalog). This ticket introduces pytest factories / factory_boy (or equivalent) plus DB reset strategy per test.

## Scope

**In:**
- Deterministic test DB configuration using `DATABASE_URL` pointing at disposable database (docker service from `[[CS-020]]`)
- Session-scoped connection fixture (`django_db`, transactional pytest-django) and function-scoped rollback **or** truncate cascade strategy—pick one, document tradeoff
- Factories for: `Project`, `ContractAnalysis` (minimal legal combination), `ContractSubmission`, `OcrJob`, `DeliveryRequest`, `RubricVersion`, `Criterion` (lightweight stub row), `CorpusVersion`, `LegalDocument`, `LegalChunk` (random dim-384 vector generator), `EconomicBenchmark`/`BenchmarkVersion`
- Helper to generate valid `public_short_id` matching `CS-YYYY-XXXXXX` pattern from DOMAIN

**Out (explicit):**
- Full-domain seed content — [[CS-033]]–[[CS-034]]
- Service integration tests — feature epics

## Acceptance criteria

- [x] Given `pytest` run, when fixture setup executes, when tests finish, then subsequent test run starts from clean schema (`python manage.py migrate` baseline or transaction rollback—document which)
- [x] Given factory for `LegalChunk`, when created, when embedding vector length inspected, then always 384 floats
- [ ] Given factory violating FK order intentionally in negative test, when developer runs example, then error message guides correct creation order
- [x] Given retention view tests from [[CS-030]] need overdue rows, when factory helper ages `expires_at` / `created_at`, when queried, then views reflect synthetic overdue counts
- [ ] Given CI from [[CS-004]] and [[CS-035]], when database job runs, when fixtures used, then runtime stays within practical budget (<2 min) via reused container

### BVA — boundary value analysis

| Input | Boundary | Expected |
|---|---|---|
| Vector generator | values unnormalized | Still accepted by pgvector column; optional L2 normalize toggle documented for realism |
| `score_total` factory default | 0 vs 10 | Both satisfy DB CHECK when paired with consistent `band` |

## Test intent (Rule 9)

Without factories, engineers bypass constraints in ad-hoc SQL and miss ORM mapping bugs. Factories encode valid defaults—tests fail when schema evolution breaks them.

## Notes

- Avoid shipping factories outside `tests/` tree.
- Consider `faker` seeds for reproducibility in parallel CI.

---

## Status — 2026-05-15

**Implemented:**
- `factory-boy` and `pytest-django` declared in `backend/pyproject.toml` `[dependency-groups].dev`. `DJANGO_SETTINGS_MODULE = "config.settings"` set in `[tool.pytest.ini_options]`.

**Not started:**
- No `conftest.py`, no `tests/` directory, no `factories.py` exist anywhere under `backend/` (verified via `find` — zero matches outside `.venv/`).
- No clean-DB strategy (transactional rollback vs truncate cascade) documented.
- No factories for any of the 12 Casa entities exist.
- No `public_short_id` generator helper.

**Follow-ups:**
- Create `backend/conftest.py` + `backend/tests/` tree with factory_boy factories for `Project`, `ContractAnalysis`, `ContractSubmission`, `OcrJob`, `DeliveryRequest`, `RubricVersion`, `Criterion`, `CorpusVersion`, `LegalDocument`, `LegalChunk` (with dim-384 vector generator), `BenchmarkVersion`, `EconomicBenchmark`.
- Pick + document rollback strategy (pytest-django `@pytest.mark.django_db(transaction=True)` vs class-scoped).
- Depends on CS-030 closure for retention-view fixtures.

---

## Status — 2026-05-16

Forensic re-audit of the closeout batch (`5be7bfd`, `a129268`).

**Implemented (audit confirmation):**
- `backend/conftest.py` documents the chosen strategy as transactional rollback per test (pytest-django default via `@pytest.mark.django_db`), with `@pytest.mark.django_db(transaction=True)` carve-out for DB-level trigger visibility (e.g. the CS-030 `reject_catalog_mutation` trigger).
- `backend/tests/factories.py` ships `DjangoModelFactory` classes for all 12 entities: `Project`, `ContractAnalysis`, `ContractSubmission`, `OcrJob`, `DeliveryRequest`, `RubricVersion`, `Criterion`, `CorpusVersion`, `LegalDocument`, `LegalChunk`, `BenchmarkVersion`, `EconomicBenchmark`. Catalogs use `django_get_or_create=("version",)` to honour the active-singleton partial unique index.
- Helpers: `random_vector_384(seed=None)` returns exactly `EMBEDDING_DIM` (=384) floats in [-1, 1]; `make_public_short_id(year=None)` returns `CS-YYYY-XXXXXX` (6 alphanumeric); `age_to(ts, on=, field='created_at')` and `expire_in(delta, on=, field='expires_at')` backdate rows past `auto_now_add` for retention-view tests.
- `backend/tests/test_smoke.py` ships `test_django_setup_works`, `test_url_conf_loads`, `test_settings_module_importable`.

**Partial / Missing:**
- AC3 (negative-test FK-order example with a "guides correct creation order" error message) — not authored.
- AC5 (CI runtime <2 min via reused container) — `.github/workflows/ci.yml::backend-test` runs but cache key only hashes `poetry.lock`; no `--reuse-db` and no timing assertion. Cannot tick.

**Flip:** stays `in_progress`. 3/5 ACs ticked.


---
id: CS-002
domain: backend
secondary_domains:
  - infra
epic: EPIC-00
status: in_progress
owner: tbd
estimate: M
depends_on:
  - CS-001
prd_refs:
  - ARCHITECTURE §4
  - BE-SERVICES §1
tags:
  - casa-segura
  - ticket
  - epic-00
  - domain/backend
  - domain/infra
---

# CS-002 — Django 5.2 LTS scaffold with pyproject, ruff, mypy, pytest

## Context

The Python service is the bulk of the system (per user direction: more BE than FE, service layer is the most important part). This ticket stands up the BE skeleton so every subsequent service ticket has a place to land its code.

## Scope

**In:**
- `apps/api/` directory hosting Django 5.2 LTS project layout (`manage.py`, settings package, URLs) exposing one minimal REST or root view stub (placeholder for [[CS-008]])
- `pyproject.toml` with Python 3.11+, Django 5.2 LTS, Django REST Framework (DRF), Celery[redis], pytest, pytest-django, ruff, black, mypy, django-stubs as base deps
- `Dockerfile` for local and prod-equivalent runtime
- `docker-compose.yml` at repo root with `api` + `postgres` + `redis` services
- `pytest.ini` or pyproject `[tool.pytest.ini_options]` with sensible defaults
- `mypy.ini` with strict mode over Django-managed packages (`apps/api`…) and lenient overrides for `tests/`
- `ruff.toml` configured matching pre-commit hooks ([[CS-005]])

**Out (explicit):**
- Any service code — feature epics
- Database connection — [[CS-022]]
- Logging — [[CS-007]]

## Acceptance criteria

- [x] `python manage.py runserver` from the documented backend root starts the HTTP server locally (`poetry run` prefix if Poetry is canonical)
- [x] `pytest` runs and finds at least one passing smoke test
- [ ] `ruff check apps/api` returns zero issues
- [ ] `mypy` over Django application packages documented in README/config returns zero blocking issues with `django-stubs` wired for strict settings
- [ ] `docker-compose up api` builds and starts the API container
- [ ] `docker-compose up` brings up `api`, `postgres`, `redis` together
- [x] Python version pinned via `.python-version` (pyenv) or equivalent

## Test intent (Rule 9)

The scaffolding is a precondition for every BE ticket. A test that boots the app catches dependency drift early; a smoke test on `/` proves the app is wireable, not just compilable.

## Notes

- Pin dependency versions, do not use unbounded ranges
- [[ARCHITECTURE]] §4 aligns with Django 5.2 LTS + Python 3.11 wherever that document names the backend stack (update ARCHITECTURE in a dedicated docs pass if wording still cites the prior framework)

---

## Status — 2026-05-15

**Implemented:**
- Django 5.2 LTS project layout with `manage.py`, settings package, URLs — `backend/manage.py`, `backend/config/settings.py`, `backend/config/urls.py` (commit `4214d88`).
- `pyproject.toml` with Python 3.11+, Django 5.2 LTS, DRF, Celery, Redis, pytest, pytest-django, factory-boy, black, isort, mypy — `backend/pyproject.toml` (commit `4214d88`/`02fac1f`). Note: ticket lists `ruff`; project uses `black + isort` instead.
- `pytest.ini` equivalent — `backend/pyproject.toml::[tool.pytest.ini_options]` sets `DJANGO_SETTINGS_MODULE = "config.settings"`.
- Django boots and exposes `/api/health/`, `/api/ready/`, admin, drf-spectacular schema, prometheus — `backend/config/urls.py`.

**Partial / Missing:**
- Path expectation: ticket specifies `apps/api/` but actual scaffold lives at `backend/` (per repo decision). Treating SPIRIT of "Does Django boot?" as met.
- Toolchain divergence: ticket lists `ruff` + `mypy strict + django-stubs`; `pyproject.toml` ships `black`/`isort`/`mypy` but no `ruff`, no `django-stubs`, and no strict-mode mypy config.

**Not started:**
- `pytest` smoke test — no `tests/` directory and no `conftest.py` found under `backend/` (verified via `find`).
- `ruff check apps/api` — `ruff` not in dependencies; no `ruff.toml`.
- `mypy` over Django packages with `django-stubs` — `django-stubs` not installed; no `mypy.ini`.
- `Dockerfile` for backend — none present in `backend/`.
- `docker-compose up api` — `docker-compose.dev.yml` only ships `postgres` + `redis`; no `api` service.
- Python version pinned via `.python-version` — file absent at repo root and in `backend/`.

**Follow-ups:**
- Add `tests/` + `conftest.py` + at least one smoke test (overlaps with CS-032).
- Add `ruff` (or document `black`/`isort` as the chosen linter and update ticket), `django-stubs`, `mypy.ini`.
- Add `backend/Dockerfile` and an `api` service to `docker-compose.dev.yml`.
- Add `.python-version` file.

---

## Status — 2026-05-16

Forensic re-audit of the closeout batch (`5be7bfd`, `a129268`).

**Implemented (new since 2026-05-15):**
- `backend/Dockerfile` multi-stage with three runtime targets `web` / `worker` / `beat` (Gunicorn; Celery worker with `tesseract-ocr` + `poppler-utils` + `libheif1`; Celery beat with `django_celery_beat:DatabaseScheduler`). `backend/.dockerignore` excludes `.venv`, tests, `.env`, build artefacts.
- `backend/.python-version` pinned to `3.11.9`.
- `backend/ruff.toml`: target `py311`, line-length 119, lint sets `E/F/W/B/UP/S/DJ/PL/RUF`, per-file ignores for `tests/`, `migrations/`, `conftest.py`.
- `backend/mypy.ini`: `plugins = mypy_django_plugin.main`, strict islands on `platform_core.domain.*` and `shared.observability.*`, migrations exempt, `django-stubs` plugin wired with `django_settings_module = "config.settings"`.
- `backend/tests/test_smoke.py` covers `test_django_setup_works`, `test_url_conf_loads`, `test_settings_module_importable`. `backend/conftest.py` documents the transactional-rollback strategy (CS-032).

**Partial / Missing (blocks remaining ACs):**
- `ruff` and `django-stubs` are NOT in `backend/pyproject.toml` `[dependency-groups].dev` (current list: `black`, `isort`, `pytest`, `pytest-django`, `pytest-asyncio`, `factory-boy`, `respx`, `mypy`). Verified live: `backend/.venv/bin/python -c "import ruff"` and `import mypy_django_plugin` both raise `ModuleNotFoundError`. `mypy.ini` references the django-stubs plugin but mypy will not be able to load it as configured. AC3 (`ruff check`) and AC4 (`mypy + django-stubs`) stay unticked.
- `docker-compose.dev.yml` still ships only `postgres` + `redis`; no `api` service references the new Dockerfile. AC5 + AC6 (`docker-compose up api`, full stack up) stay unticked.

**Flip:** stays `in_progress`. 3/7 ACs ticked (runserver, smoke pytest, `.python-version`).


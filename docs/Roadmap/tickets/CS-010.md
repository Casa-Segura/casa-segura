---
id: CS-010
domain: misc
secondary_domains:
  - backend
epic: EPIC-00
status: in_progress
owner: tbd
estimate: S
depends_on:
  - CS-007
  - CS-009
prd_refs:
  - EPIC-00-foundation §Definition of done
  - PRD_GENERAL §4 BR-16
  - FEATURES_MAP §4 (version references in analyses)
tags:
  - casa-segura
  - ticket
  - epic-00
  - domain/misc
  - domain/backend
---

# CS-010 — Versioning ADR (rubric, corpus, API schema)

## Context

Reproducible analyses require immutable catalog versions ([[PRD_GENERAL]] BR-16) and explicit API compatibility discipline. This ticket captures an ADR explaining how `rubric_version`, `corpus_version`, `benchmark_version`, and API `schema_version` bump and how clients discover them ([[FEATURES_MAP]] §4, [[DOMAIN_MODEL]] §3).

## Scope

**In:**
- `docs/adr/ADR-00XX-versioning.md` (or repo-standard ADR folder) with: motivation, decision, consequences, rollback guidance
- Semver rules for `schema_version` returned on `/health` and error envelopes ([[CS-008]], [[CS-009]])
- Mapping table: where versions are stored at rest (`contract_analysis` columns) vs log fields ([[CS-007]])
- Process: feature PR must bump `schema_version` when JSON response contracts change; catalog bumps require new rows per PRD immutability (pointer to [[PRD_F8_PERSISTENCIA_PROYECTO_RETENCION]] / EPIC-01 seeds)

**Out (explicit):**
- Actual seed rows — [[CS-033]], [[CS-034]]
- CI enforcement automation — optional follow-up

## Acceptance criteria

- [x] Given the ADR file, when a reviewer reads it, then they can answer: (1) when to bump API `schema_version`, (2) who may publish new rubric/corpus/benchmark versions, (3) how clients log incompatible versions
- [x] Given [[CS-008]] implementation, when `/health` JSON is compared to ADR, then the `schema_version` field semantics match the documented meaning
- [x] Given [[CS-009]] error envelope, when `schema_version` appears, then ADR documents whether errors must echo request or deployment version (explicit decision recorded)
- [x] Given immutability rules in PRD F8 / DOMAIN, when ADR references them, then it states that mutating published catalogs in-place is forbidden; only new version rows are allowed
- [ ] Given onboarding README, when a subsection links ADRs, then this ADR appears in the index list

### BVA — boundary value analysis

| Input | Boundary | Expected policy in ADR |
|---|---|---|
| `schema_version` change | PATCH vs MINOR vs MAJOR | Document mapping to client breakage (additive JSON field vs renamed field) |
| Log `rubric_version` unset | health check path | Allowed `null`; analysis paths must be non-null by EPIC-06/07 completion |

## Test intent (Rule 9)

Without a written ADR, engineers will silently couple FE/BE on implicit JSON or reuse version strings for unrelated concepts. The ADR is the test oracle: future PRs that contradict it should fail review.

## Notes

- Reference [[BE-SERVICES]] stack choices only where they affect embedding dimension + `corpus_version` coupling (384-d MiniLM).
- Keep ADR short; defer operational backup/versioning to [[EPIC-11-observability]] if needed.

---

## Status — 2026-05-15

**Implemented:**
- (none — no versioning ADR file exists.)

**Partial / Missing:**
- ADRs present in `docs/adr/` cover the Django backend stack (ADR-0001), the platform→platform_core rename (ADR-0002), and the CS-031 normalization divergence (ADR-0003). None of them define the versioning semantics for `schema_version`, `rubric_version`, `corpus_version`, `benchmark_version`, semver bump rules, or the publish-new-row immutability policy required by this ticket.
- `schema_version="1.0.0"` is hardcoded in `backend/shared/observability/logging.py::SCHEMA_VERSION` and surfaced by `/health` (CS-008) and the error envelope (CS-009), so the runtime baseline exists — but the policy ADR is missing.

**Not started:**
- All five acceptance criteria.

**Follow-ups:**
- Author `docs/adr/ADR-0004-versioning.md` per ticket scope: when to bump `schema_version` (PATCH/MINOR/MAJOR), who may publish new rubric/corpus/benchmark rows, immutability rules from PRD F8.
- Wire the new ADR into onboarding README index (depends on CS-001).

---

## Status — 2026-05-16

Forensic re-audit of the closeout batch (`5be7bfd`, `a129268`).

**Implemented (audit confirmation):**
- `docs/adr/ADR-0004-versioning.md` exists, status `Accepted`, dated 2026-05-15. Covers all four identifiers (`schema_version` semver, `rubric_version` semver, `corpus_version` ISO date, `benchmark_version` quarterly tag `YYYY-Q{1..4}`) in a decision table with format, bump cadence, mutability. Bump procedure documented (author seed → idempotent verify → PR + product sign-off → atomic `is_active` flip via paired UPDATE). Ties to CS-030 `reject_catalog_mutation()` and the `uq_<catalog>_version_active_singleton` partial unique index. Alternatives section rejects single-`app_version`, audit-only mutability, and git-SHA versioning.

**Partial / Missing:**
- AC5: root `README.md` ADR index lists ADR-0001 only (no ADR-0002/0003/0004 either) — `grep` confirms. The PHASE-0 phase index does link ADR-0001/0002/0003 but not ADR-0004.

**Flip:** `backlog` → `in_progress`. 4/5 ACs ticked. One-line README ADR-list edit closes the ticket.


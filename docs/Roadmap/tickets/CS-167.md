---
id: CS-167
epic: EPIC-06
status: backlog
owner: tbd
estimate: S
depends_on:
  - CS-156
prd_refs:
  - RUBRICA_CONTRATO §2.1
  - PRD_GENERAL US-03
domain: qa
tags:
  - casa-segura
  - ticket
  - epic-06
  - domain/qa
---

# CS-167 — BVA tests: score band boundaries

## Context

Bands partition user messaging (**red / yellow / green**) at **4.9|5.0** and **7.9|8.0** inclusive edges ([[RUBRICA_CONTRATO]] §2.1). Automated tests must freeze rounding + boundary semantics independent of UI ([[Roadmap/README]] #5).

## Scope

**In:**
- Parameterized pytest / table tests hitting `band_from_total` directly ([[CS-156]])
- Integration smoke feeding synthetic category averages yielding totals **exactly** at boundaries post-`round(score_total,1)` ([[CS-152]])
- Regression vectors when overrides inactive

**Out (explicit):**
- Hex color palette assertions — presentation layer

## Acceptance criteria

- [ ] Table below encoded as automated cases **pass** (names map to doc ids):

### Concrete boundary table (authoritative fixtures)

| `score_total` (after round half-up half-down policy documented in [[CS-152]]) | Expected `band` |
|---|---|
| 0.0 | red |
| 4.9 | red |
| 5.0 | yellow |
| 7.9 | yellow |
| 8.0 | green |
| 10.0 | green |

- [ ] Floating pipeline case: internal value **7.949999** → rounded **7.9** → **yellow**; **7.950000** → **8.0** → **green** (adjust if project picks banker's rounding — **must match [[CS-152]]**)
- [ ] Given override active, bypass path tested in [[CS-170]] / [[CS-157]] — this ticket asserts helper ignores override flag (unit isolation)

### BVA — boundary value analysis

| Pre-round raw | Rounded | Band |
|---|---|---|
| 4.949 | 4.9 | red |
| 4.950 | 5.0 | yellow |
| 7.949 | 7.9 | yellow |
| 7.950 | 8.0 | green |

## Test intent (Rule 9)

A future engineer tightening floats must not accidentally map **5.0** to red — tests cite §2.1 numeric literals as comments pinned to [[RUBRICA_CONTRATO]].

## Notes

- If analytics duplicates bands, import single function — avoid drift ([[EPIC-11-observability]] future).

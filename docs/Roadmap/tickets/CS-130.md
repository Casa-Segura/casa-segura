---
id: CS-130
epic: EPIC-05
status: backlog
owner: tbd
estimate: M
domain: data
secondary_domains:
  - backend
  - qa
depends_on:
  - CS-028
prd_refs:
  - PRD_F5_ANALISIS_ECONOMICO §6.4
  - PRD_F5_ANALISIS_ECONOMICO US-03
  - PRD_GENERAL BR-13
  - RUBRICA_CONTRATO §14
  - RUBRICA_CONTRATO §14.1
  - DOMAIN_MODEL §3.8
tags:
  - casa-segura
  - ticket
  - epic-05
  - domain/data
  - domain/backend
  - domain/qa
---

# CS-130 — Define economic_benchmarks.yaml schema and seed values

## Context

Market comparisons must be reproducible and source-attributed ([[PRD_GENERAL]] BR-13). This ticket establishes the repo-owned `economic_benchmarks.yaml` matching [[PRD_F5_ANALISIS_ECONOMICO]] §6.4 (top-level `version`, `released_at`, `next_review_due`, `benchmarks` map) while aligning keys with rubric narratives in [[RUBRICA_CONTRATO]] §14 — noting illustrative-vs-production caveat in §5 intro — and prepares loader compatibility with [[DOMAIN_MODEL]] `EconomicBenchmark`.

## Scope

**In:**
- YAML schema contract (documented + validated at load): required benchmark entries including minimum keys from [[PRD_F5_ANALISIS_ECONOMICO]] US-03 table (`standard_down_payment_bank_purchase`, mortgage min/max/mid, FSV min/max, developer_direct min/max, `credit_term_max_reasonable`, `credit_term_ivu_max`, cost multiplier trio, monthly-payment ratio trio when wired).
- Each benchmark row includes `unit`, `applicable_contract_types`, `source`, `last_updated`, `next_review_due`, optional `source_url`, `notes`.
- Semantic version tag like `2026-Q2` per examples across [[PRD_F5_ANALISIS_ECONOMICO]] §6.4 and §6.2 `benchmark_version` table expectations.
- Startup validation: malformed YAML ⇒ fatal error with actionable message ([[PRD_F5_ANALISIS_ECONOMICO]] US-03).

**Out (explicit):**
- Legal verification that each numeric equals live regulatory feeds ([[RUBRICA_CONTRATO]] §17 open decision — marks seed as provisional until ABANSA/BCR sign-off).
- Runtime reload plumbing ([[PRD_F5_ANALISIS_ECONOMICO]] §6.3 `BENCHMARK_RELOAD_ON_SIGHUP`) — optional stub acceptable if ticket split later.

## Acceptance criteria

- [ ] Repository file exists at configurable path defaulting per [[PRD_F5_ANALISIS_ECONOMICO]] §6.3 `BENCHMARK_FILE_PATH` with `version`, `released_at`, `next_review_due`, and populated `benchmarks` map covering all **Required benchmarks (minimum list)** rows in [[PRD_F5_ANALISIS_ECONOMICO]] US-03 (source gaps must be written as `source_status: pending_verification` in `notes`; prefer numeric seeds from §6.4 exemplar where present).
- [ ] Loader rejects: missing required fields, unknown `unit`, empty `applicable_contract_types`, or `next_review_due` before `last_updated`.
- [ ] `bank_mortgage_rate_mid` documents derivation rule ("Average of min and max") inline in YAML `notes` matching US-03.
- [ ] Cost multiplier keys align rubric naming: `cost_multiplier_healthy_max`, `cost_multiplier_high_max`, `cost_multiplier_excessive_min` with values consistent with [[RUBRICA_CONTRATO]] §14 `total_cost_multiplier` block (`healthy_max` 1.5, `high_max` 2.0, `excessive_min` 2.5) — if [[RUBRICA_CONTRATO]] §5 B4 scale (1.8 band) diverges, document explicit mapping decision in Notes for product sign-off.

### BVA — boundary value analysis

| Field | Boundary | Expected |
|---|---|---|
| `unit` | `pct` vs `ratio` vs `months` | Loader enforces allowed enum union per [[PRD_F5_ANALISIS_ECONOMICO]] §5.1 SQL check (mirrored in YAML validator) |
| `applicable_contract_types` | Empty array | Reject at load |

## Test intent (Rule 9)

Fixture tests prove **benchmark file version string** propagates — if an engineer edits `version` without updating tests, reproducibility regressions surface ([[PRD_GENERAL]] BR-13).

## Notes

- [[STATUS_RECONCILIATION]] reminds B4 band edges (1.5/1.8/2.0/2.5) differ from §14 YAML (1.5/2.0/2.5) — escalate to product; ticket seeds follow F5 §6.4 + RUBRICA §14 until resolved.
- DB sync to `economic_benchmark` table can be feed job or migration seed — coordinate with [[CS-028]].

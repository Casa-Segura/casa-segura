---
id: CS-132
epic: EPIC-05
status: done
owner: tbd
estimate: S
domain: data
secondary_domains:
  - qa
depends_on:
  - CS-130
  - CS-113
prd_refs:
  - PRD_F5_ANALISIS_ECONOMICO US-02
  - RUBRICA_CONTRATO §5 B5
  - RUBRICA_CONTRATO §14 monthly_payment
tags:
  - casa-segura
  - ticket
  - epic-05
  - domain/data
  - domain/qa
---

# CS-132 — Compute monthly payment alignment ratio

## Context

[[RUBRICA_CONTRATO]] §5 B5 compares monthly installment to linear baseline `monthly_payment ÷ (price_cash / term_months)`. [[RUBRICA_CONTRATO]] §14 exposes configurable ratio rails (`ratio_healthy_max` 1.2, `ratio_high_max` 1.4, `ratio_excessive_min` 1.7). This ticket computes the ratio and attaches comparisons for downstream scoring/report ([[EPIC-06-rubric-engine]]) without interpreting rubric weights.

## Scope

**In:**
- Inputs: validated `monthly_payment`, `price_cash`, `term_months` (>0).
- Formula: `ratio = monthly_payment / (price_cash / term_months)` when denominator > 0.
- Emit structured metric `{ratio, baseline_monthly_linear, inputs_snapshot}` inside economics intermediate bundle.
- Compare computed ratio against benchmarks loaded from [[CS-130]] keys mirroring §14 (`monthly_payment.ratio_*`) producing preliminary assessment tags mirroring lexicon in [[DOMAIN_MODEL]] §5.6 (`within_market`, etc.) **only if** product maps ratios to assessments — otherwise expose numeric only.

**Out (explicit):**
- Income-based affordability (explicitly out of product scope).

## Acceptance criteria

- [x] Given `price_cash = 80000`, `term_months = 240`, `monthly_payment = 1083.50`, ratio equals `1083.50 / (80000/240)` within numeric tolerance policy shared with [[CS-133]]. _Shipped in `compute_monthly_ratio`. Golden vector `test_golden_vector_from_ticket` asserts ratio == Decimal("3.2505") (quantized to 0.0001 with `ROUND_HALF_EVEN`)._
- [x] Given missing any input component, ratio computation is skipped and reason recorded (`insufficient_inputs`). _Two skip reasons exported: `insufficient_inputs` (any of price / term / monthly missing or non-PRESENT) and `zero_term` (denominator collapses). Covered by `test_any_missing_slot_skips` (parametrized) + `test_term_zero_skips_with_zero_term_reason`._
- [x] Given benchmarks present in YAML for ratio rails, evaluations compare ratio against configured thresholds producing discrete band enums documented beside YAML ([[RUBRICA_CONTRATO]] §14). _`MonthlyRatioBand` StrEnum (HEALTHY / ELEVATED / HIGH / EXCESSIVE / SKIPPED). Thresholds passed in by the assembler (CS-135) from `monthly_payment_ratio_healthy_max`, `_high_max`, `_excessive_min`. BVA at each rail edge covered by `test_band_classification_at_rails`._

### BVA — boundary value analysis

| Ratio vs rails | Boundary | Expected band |
|---|---|---|
| 1.199 vs 1.201 | vs `ratio_healthy_max` (~1.2) | Below/at healthy vs enters elevated band |
| 1.699 vs 1.701 | vs `ratio_excessive_min` (~1.7) | High vs excessive |
| Denominator | `term_months = 0` | Skip + invalid reason |

## Test intent (Rule 9)

Tests anchor **division order** — swapping numerator/denominator must fail because B5 definition is asymmetric ([[RUBRICA_CONTRATO]] §5 B5).

## Notes

- Assessment naming parity with rate deltas deferred if ratio lacks textual assessment in F5 PRD — expose numeric regardless for rubric LLM consumption.

## Status — 2026-05-16

**Shipped (status flipped `backlog → done`):**

- `backend/economics/domain/payment_ratio.py` — `RatioComputation` DTO + `MonthlyRatioBand` StrEnum (HEALTHY / ELEVATED / HIGH / EXCESSIVE / SKIPPED). Pure domain.
- `backend/economics/application/payment_ratio.py` — `compute_monthly_ratio(extraction, *, healthy_max, high_max, excessive_min) -> RatioComputation`. Asymmetric formula `monthly / (price/term)` with `_classify()` mapping per the inclusive-lower-bound convention from RUBRICA §5 B5. Quantized to 0.0001 via `ROUND_HALF_EVEN`.
- `backend/tests/test_economics_payment_ratio.py` — 17 tests: golden vector from the ticket (3.2505), Rule 9 division-order guard, BVA at all three rail edges (0.001 above/below 1.2 / 1.4 / 1.7), three skip paths (insufficient inputs parametrized, zero_term, AMBIGUOUS slot, zero/negative price).

**Verification:**

```
poetry run pytest tests/test_economics_payment_ratio.py -q
# → 17 passed
poetry run ruff check economics/application/payment_ratio.py economics/domain/payment_ratio.py
# → All checks passed!
```

---
id: CS-159
epic: EPIC-06
status: done
owner: tbd
estimate: M
depends_on:
  - CS-158
prd_refs:
  - RUBRICA_CONTRATO §4, §16 (A1–A6)
  - PRD_F4_MOTOR_RUBRICA §8.1
domain: backend
secondary_domains:
  - ai-rag
tags:
  - casa-segura
  - ticket
  - epic-06
  - domain/backend
  - domain/ai-rag
---

# CS-159 — Category A evaluators (A1–A6)

## Context

Category **A** (20% global weight) covers legal/formal validity: deed form, parties, determined price, term, signatures, and promise-to-sell rules ([[RUBRICA_CONTRATO]] §4). Overrides **art_1605_cc**, **art_1613_cc**, **art_1425_cc** surface here.

## Scope

**In:**
- Registered evaluators `A1`…`A6` consuming prompts + scales from [[CS-033]] rows only (no duplicated YAML in code)
- Override detectors aligned with rubric: A1/A3/A6 emit appropriate `override_triggered` when textual pattern matches critical definitions ([[RUBRICA_CONTRATO]] §4)
- Evidence snippets extracted verbatim Spanish clauses ≤500 chars ([[PRD_F4_MOTOR_RUBRICA]] BR-16)

**Out (explicit):**
- OCR quality remediation — [[EPIC-02-ingestion-ocr]]
- Public deed verification against Registro — [[PRD_GENERAL]] out of scope

## Acceptance criteria

- [ ] Given fixture CVP missing public deed for sale element, A1 returns score consistent with scale row **2** *and* sets `override_triggered=art_1605_cc` when rubric condition met ([[RUBRICA_CONTRATO]] A1 scale + override)
- [ ] Given discretionary pricing clause, A3 returns score **0** with `art_1613_cc`
- [ ] Given APV lacking execution term/condition, A6 triggers `art_1425_cc` with rubric score **0**
- [ ] Given lease-only ARV contract, purchase-only fields in A1/A3 evaluated per lease branch without forcing deed requirement incorrectly
- [ ] Each evaluator references PRD scale IDs in justification metadata field `scale_anchor` for QA diff review

### BVA — boundary value analysis

| Criterion | Boundary inputs | Expected bucket |
|---|---|---|
| A1 | deed present vs simple private purchase doc | 10 vs path to override |
| A2 | missing one DUI vs two fields missing | 7 vs 4 per scale |
| A4 | term equals legal max vs term missing | 10/6 vs 0 |
| A5 | two of three signature/date/place vs none | 6 vs 0 |
| A6 | three of four Art. 1425 circumstances missing | map to 7/4/0 ladder |

## Test intent (Rule 9)

Override-linked criteria must never silently downgrade from **critical** to numeric commentary — tests fail if `override_triggered` absent when rubric says forced zero ([[PRD_GENERAL]] US-03).

## Notes

- [[EPIC-06-rubric-engine]] DoD: every §16 row has evaluator — this ticket closes rows **A1–A6**.
- Spanish prompts — [[PRD_F4_MOTOR_RUBRICA]] §8.

---
id: CS-021
domain: data
secondary_domains:
  - infra
epic: EPIC-01
status: in_progress
owner: tbd
estimate: S
depends_on:
  - CS-020
prd_refs:
  - PRD_F8_PERSISTENCIA_PROYECTO_RETENCION US-01
  - BE-SERVICES §2.4
  - FEATURES_MAP §3 Phase 0
tags:
  - casa-segura
  - ticket
  - epic-01
  - domain/data
  - domain/infra
---

# CS-021 — Django migrations bootstrap + baseline migration framework

## Context

Schema evolution must be versioned ([[PRD_F8_PERSISTENCIA_PROYECTO_RETENCION]] US-01; [[BE-SERVICES]] §2.4). This ticket establishes Django migrations in `apps/api` (or documented backend root, `DJANGO_SETTINGS_MODULE` discoverable via `manage.py`), wired to `DATABASE_URL` from [[CS-006]], ahead of per-entity migrations in [[CS-023]]–[[CS-029]].

## Scope

**In:**
- Django 5.2 LTS `INSTALLED_APPS` layout with an initial migration chain (empty/minimal revisions only if convention requires placeholders)
- Makefile/README targets: `python manage.py migrate`, `python manage.py showmigrations`; reverse flow documented (`migrate <app_label> zero` or equivalent rollback policy for disposable DBs) for alignment with CI in [[CS-035]]
- CI hook placeholder documented for [[CS-035]]

**Out (explicit):**
- Business tables — [[CS-023]] onward
- Autogenerate trust without human review — disallow blind auto-merges

## Acceptance criteria

- [x] Given empty relational database excluding Django’s baseline, when `python manage.py migrate` runs, then command exits zero and `django_migrations` records applied revisions
- [x] Given applied migrations, when the documented rollback sequence completes on a disposable database, then schema objects created by Casa migrations revert consistent with revision definitions (Django contrib tables may persist—document expectation)
- [ ] Given README, when developer follows DB section, then commands work from a single documented working directory
- [x] Given `.env` with `DATABASE_URL`, when Django loads settings, then it resolves that URL without accidental default SQLite fallback
- [ ] Given migration file naming convention, when new revisions are added later, then template enforces descriptive slugs + numeric prefixes per team policy

## Test intent (Rule 9)

Django migrate misconfiguration (wrong URL, missing extension) blocks every schema ticket. A smoke command that upgrades an ephemeral Postgres + pgvector instance is the minimal proof the migration tool chain is real.

## Notes

- Prefer single source of truth for `DATABASE_URL` between `manage.py migrate` runtime and Django ORM from [[CS-022]].
- Coordinate `CREATE EXTENSION vector` placement: either earliest data migration touching vector fields or explicitly paired with first `legal_chunk` migration per engineering choice—document decision.

---

## Status — 2026-05-15

**Implemented:**
- `INSTALLED_APPS` with all 9 Casa apps + `MIGRATION_MODULES` mapping each app to `<app>.infrastructure.django.migrations` — `backend/config/settings.py` lines 40-48 and 121-131 (commit `02fac1f`).
- Per-app `0001_initial.py` migrations exist: `common`, `rubric`, `corpus`, `economics`, `platform_core`, `ingestion`, `delivery` (verified via `find`).
- `python manage.py migrate` succeeded against `casasegura` Postgres — verified via `psql \dt` (32 tables present including `django_migrations`, all 12 Casa business tables).
- `DATABASE_URL` resolved from `backend/.env` via `django-environ`; settings explicitly guard against silent SQLite fallback unless `DB_ENGINE=sqlite` set — `backend/config/settings.py` lines 95-118.

**Partial / Missing:**
- `CREATE EXTENSION` placement is **outside** Django migrations — extensions are bootstrapped by `infra/postgres/init/01-extensions.sql` (runs once on first Docker volume init), not by a `RunSQL` migration. Decision is not documented in an ADR or README.
- Reverse-migration smoke (`migrate <app> zero`) — not exercised in CI (no CI exists; see CS-035).
- No README/Makefile with `migrate`/`showmigrations` targets — root README absent.

**Not started:**
- Migration naming-convention template/policy doc.

**Follow-ups:**
- Document the "extensions live in `infra/postgres/init/`, not in migrations" decision (mini-ADR or README ops section).
- Add a Makefile or README cheatsheet for `python manage.py migrate`/`showmigrations`/rollback.
- Depends on CS-035 to verify revertibility in CI.

---

## Status — 2026-05-16

Forensic re-audit of the closeout batch (`5be7bfd`, `a129268`).

**Implemented (audit confirmation):**
- `backend/Makefile` ships `migrate`, `makemigrations`, `migrations-check` (`--check --dry-run`), `rollback` (parameterised `APP=… TO=…`), `fresh-db` (drop + create + `CREATE EXTENSION vector/pgcrypto/uuid-ossp` + migrate), `seed-rubric` / `seed-corpus` (call the CS-033/CS-034 commands), `check`, `format`, `lint`, `type-check`, `test`, `runserver`, `shell`, `celery-worker`, `celery-beat`, `dev-up/-down/-logs`.
- Reverse-migration sequence wired in CI: `.github/workflows/ci.yml::backend-migration-smoke` loops `migrate <app> zero` over `delivery → ingestion → economics → rubric → corpus → platform_core → common`, then re-migrates forward (CS-035 also covers this).
- Live DB verification (53 rows of `django_migrations`): the Phase-0 closeout migrations are applied — `corpus 0002_fk_doc_hnsw_immutability`, `platform_core 0002_immutability_function_and_views`, `platform_core 0003_fix_immutability_function`, `rubric 0002_criterion_idx_criterion_types`, `rubric 0003_attach_immutability`, `economics 0002_attach_immutability`. All 9 Casa apps + the 4 Django contrib + softdelete + django_celery_beat present.

**Partial / Missing:**
- AC3 (README "DB section" with single documented working directory) — root `README.md` lists "install dependencies, run migrations" as prose; no `make migrate` cheatsheet, no explicit cwd note. No `backend/README.md` exists.
- AC5 (migration naming-convention template / policy doc) — not authored.

**Flip:** stays `in_progress`. 3/5 ACs ticked.


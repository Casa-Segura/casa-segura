---
id: CS-004
domain: infra
epic: EPIC-00
status: backlog
owner: tbd
estimate: M
depends_on:
  - CS-002
  - CS-003
prd_refs:
  - EPIC-00-foundation §Definition of done
  - ARCHITECTURE §9
tags:
  - casa-segura
  - ticket
  - epic-00
  - domain/infra
---

# CS-004 — CI pipeline (lint, type-check, test, build) on PR

## Context

[[EPIC-00-foundation]] requires every PR to prove both apps stay green. This ticket adds continuous integration so merges are blocked on measurable quality gates, matching the open-source posture in [[ARCHITECTURE]] §9.

## Scope

**In:**
- CI workflow(s) for pull requests (e.g. GitHub Actions) running on `apps/api` and `apps/web`
- Jobs: Python `ruff`/`mypy`/`pytest` as configured in [[CS-002]]; web `lint`, `typecheck`, `test`, `build` as configured in [[CS-003]]
- Branch protection documentation: required checks listed in README or contributing doc (if org permits)
- Caching for package installs where straightforward (pip/npm/pnpm)

**Out (explicit):**
- Deploy pipelines — [[EPIC-11-observability]] / operations
- Database migration smoke — [[CS-035]]
- E2E/browser automation — future EPIC tickets

## Acceptance criteria

- [ ] Given an open PR that touches only `apps/api`, when CI runs, then API lint, type-check, and tests execute and their status is reported on the PR
- [ ] Given an open PR that touches only `apps/web`, when CI runs, then web lint, type-check, tests, and production build execute with reported status
- [ ] Given a PR that touches both apps, when CI runs, then both matrices complete and a single “all required checks passed” outcome is achievable
- [ ] Given a deliberate ruff violation committed on a throwaway branch, when CI runs, then the workflow fails and the failure log names the violated rule
- [ ] Given the default branch HEAD after this ticket merges, when CI runs on it, then all jobs are green without skipped “must-pass” steps

## Test intent (Rule 9)

CI is the mechanical guarantee that [[EPIC-00-foundation]]’s “types check clean, tests run green” is true on every change, not only on an engineer’s laptop. A test of the workflow itself is the repo’s contract with contributors.

## Notes

- Keep workflow YAML minimal; shard jobs only if cold-start time forces it.
- Document how forks should run checks (open source expectation).

---

## Status — 2026-05-15

**Implemented:**
- (none)

**Not started:**
- All acceptance criteria — no `.github/workflows/` directory exists at the repo root. No CI workflow file of any kind found.

**Follow-ups:**
- Author `.github/workflows/ci.yml` with backend (black/isort/mypy/pytest) and frontend (lint/test/build) jobs.
- Depends on CS-002 (backend test harness) and CS-003 (frontend tests/build) being green locally first.


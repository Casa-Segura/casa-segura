---
id: CS-208
epic: EPIC-07
status: backlog
owner: tbd
domain: backend
secondary_domains:
  - security-privacy
estimate: S
depends_on:
  - CS-200
prd_refs:
  - RUBRICA_CONTRATO §11.1 Section 8
  - PRD_F6_GENERACION_REPORTE §3 US-10
  - PRD_F6_GENERACION_REPORTE §4 BR-07
  - PRD_F6_GENERACION_REPORTE §4 BR-11
  - PRD_F6_GENERACION_REPORTE §4 BR-12
tags:
  - casa-segura
  - ticket
  - epic-07
  - domain/backend
  - domain/security-privacy
---

# CS-208 — Render footer disclaimers, version stamps, integrity hash

## Context

The footer crystallizes reproducibility commitments and reinforces non-advice framing ([[RUBRICA_CONTRATO]] §11.1 Section 8). It surfaces rubric/corpus/benchmark lineage from the frozen `ContractAnalysis` row—not live config—and exposes an integrity fingerprint users can correlate with outbound messages ([[PRD_F6_GENERACION_REPORTE]] §3 US-10).

## Scope

**In:**
- Extended disclaimer paragraph echoing §11 closing guidance + operative BR-07 text variants as required
- Version line template: `"Generado con rúbrica {rubric_version}, corpus {corpus_version}, benchmarks {benchmark_version}"` sourcing exclusively persisted fields ([[PRD_F6_GENERACION_REPORTE]] §4 BR-03)
- Deterministic truncated hash caption (`Hash del análisis: ____`) computed over canonical serialization of emitted HTML excluding volatile timestamp lines if flagged optional per §10 open Q on idempotency — **document exclusion list** codified alongside implementation; fallback includes timestamp ONLY if BR requires audit trail explicit
- Error reporting pathway citing `public_short_id` + `errores@casasegura.sv` ([[PRD_F6_GENERACION_REPORTE]] §4 BR-11)
- Conditional BR-10 sentence when project's historical analysis stats available without leaking other submissions (only aggregate count + avg score surfaced)

**Out (explicit):**
- Digital signature cryptography ([[PRD_F6_GENERACION_REPORTE]] §10 Q6 defer)
- Persisting hashes to DB (observability log optional per §5 suggested table—not mandatory here)

## Acceptance criteria

- [ ] Given `ContractAnalysis` with explicit triple of versions + optional `benchmark_version` missing ⇒ render fails closed with deterministic error surfaced to orchestrator matching §3 US-10 "explicit error when versions unavailable"
- [ ] Given two sequential renders seconds apart sharing same analysis snapshot + deterministic mode flag, hashed HTML fingerprints match bitwise after normalization policy OR documented variance limited to sanctioned fields with tests naming them
- [ ] Footer includes both header-disclaimer thematic cousin text (distinct copy block) reinforcing non-replacement for professional counsel

### BVA — boundary value analysis

| Field | Boundary | Expected |
|---|---|---|
| `benchmark_version` | present vs absent | absent blocks render (explicit failure) pending data model extension |
| `project.total_analyses` | ≤1 vs >1 | ≤1 hides BR-10 aggregate line; >1 shows aggregate respecting privacy constraints |

## Test intent (Rule 9)

Mis-labeling corpus/rubric fingerprints undermines reproducibility auditing promised to watchdogs/clients; asserting static placeholder strings must fail versus dynamic incorrect versions drawn from stale config objects.

## Notes

- Harmonize capitalization with Spanish typography decisions from copy deck
- If `report_template_version` optional column present, optionally append substring `plantilla={value}` gated by persistence availability

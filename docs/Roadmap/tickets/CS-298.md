---
id: CS-298
epic: EPIC-10
status: backlog
owner: tbd
estimate: S
depends_on:
  - CS-290
domain: infra
secondary_domains:
  - frontend
prd_refs:
  - UI-UX §7 Implementation notes
  - EPIC-10 Definition of done
tags:
  - casa-segura
  - ticket
  - epic-10
  - domain/infra
  - domain/frontend
---

# CS-298 — Vercel deployment for frontend app

## Context

[[UI-UX Documents/UI-UX]] §7 targets **Vercel** hosting. [[EPIC-10-frontend]] DoD requires deployed production (preview/staging counts as interim only if defined).

## Scope

**In:**
- Vercel project connected to repo; `main` → production, PRs → preview
- Environment variables configured for [[CS-296]] (server-only secrets)
- `next.config` security headers baseline (CSP report-only or strict per security ticket—but at minimum `X-Content-Type-Options`, `Referrer-Policy`)
- Domain wiring plan documented (may be DNS follow-on; ticket accepts vercel.app host MVP)

**Out (explicit):**
- Backend deployment — infra/EPIC-00
- Custom WAF rules — EPIC-11

## Acceptance criteria

- [ ] Given push to `main`, when CI completes, when Vercel builds, then deploy succeeds with green build logs
- [ ] Given production URL, when curling `/`, when checked, then returns `200` and renders [[CS-290]]
- [ ] Given missing env `CASASEGURA_API_BASE_URL`, when build-time or runtime check executes, then deploy fails loudly in preview with checklist message
- [ ] Given `robots.txt` policy, when reviewed, then staging/preview disallow crawlers if exposing WIP (product decision documented)
- [ ] Given rollback scenario, when redeploy previous Vercel deployment, then documented in runbook snippet in PR description

### BVA — boundary value analysis

| Input | Boundary | Expected |
|---|---|---|
| Node runtime | version pinned | Matches Next.js support matrix in repo README |
| Region | default vs latency | Acceptable TTFB on 4G Salvadoran users documented qualitatively |
| Env | preview vs prod | Preview cannot use prod BE without explicit override |

## Test intent (Rule 9)

Misconfigured deploys leak **secrets to the browser** or ship broken env splits. Tests must fail if `NEXT_PUBLIC_` is used for privileged API keys or if production points at dev backend unintentionally.

## Notes

- Link previews for WhatsApp may require domain allowlisting—coordinate with marketing when custom domain live
- Keep observability logs free of uploaded filenames with PII ([[PRD_GENERAL]] §6)

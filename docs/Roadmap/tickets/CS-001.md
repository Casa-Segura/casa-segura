---
id: CS-001
domain: misc
secondary_domains:
  - product-content
epic: EPIC-00
status: in_progress
owner: tbd
estimate: S
depends_on: []
prd_refs:
  - ARCHITECTURE §9
  - PRD_GENERAL §10
tags:
  - casa-segura
  - ticket
  - epic-00
  - domain/misc
  - domain/product-content
---

# CS-001 — Initialize public repo with MIT license and README skeleton

## Context

Casa Segura is open source from day 0 ([[ARCHITECTURE]] §9). The repo's existence and posture is the trust contract — every downstream ticket assumes a public repo with a known posture. This ticket creates that surface.

## Scope

**In:**
- Public GitHub repo under an org (decide org name in [[STATUS]] open questions if not already chosen)
- MIT LICENSE file
- README.md skeleton: what, why, scope (El Salvador only), disclaimer block, contributing pointer, link to PRDs once they move into repo
- `.gitignore` for Python and Node

**Out (explicit):**
- CI configuration — [[CS-004]]
- Code scaffolds — [[CS-002]], [[CS-003]]
- Security disclosure document — [[CS-335]]

## Acceptance criteria

- [ ] Repo URL is public and resolvable without auth
- [x] LICENSE file is exactly MIT, copyright line names the org and year
- [x] README opens with: project name, one-sentence purpose, "no es asesoría legal" disclaimer block, link to PRDs
- [x] `.gitignore` covers `.env`, `__pycache__`, `node_modules`, `.next`, `dist`, `*.pyc`
- [ ] First commit signed if commit-signing is the team policy (decide and document)

## Test intent (Rule 9)

The disclaimer block in the README is a load-bearing element of the product's legal posture per [[PRD_GENERAL]] BR-07. A test (visual review at minimum) must catch its removal in any future commit.

## Notes

- [[ARCHITECTURE]] §9 Open source posture and §10 Privacy posture both inform the README copy
- The disclaimer literal "Esto no es asesoría legal" should be one of the strings managed by [[CS-337]] once the disclaimer registry exists

---

## Status — 2026-05-15

**Implemented:**
- LICENSE file is exactly MIT, copyright line names the org and year — `LICENSE` lines 1-3 (MIT, "Copyright (c) 2026 Steven Granados"). (commit `55fd5b4`)
- `.gitignore` covers `.env`, `__pycache__`, `node_modules`, `.next`, `dist`, `*.pyc` — `.gitignore` covers all listed patterns plus a broader set (commit `3812845`/`8f8474a`).

**Partial / Missing:**
- Repo URL is public and resolvable without auth — cannot verify from local clone; depends on GitHub repo posture.

**Not started:**
- README opens with: project name, purpose, "no es asesoría legal" disclaimer block, link to PRDs — no root `README.md` exists (only `frontend/README.md` which is the default create-next-app boilerplate; no Casa Segura disclaimer block).
- First commit signed if commit-signing is the team policy — no policy documented; recent commits not GPG-signed.

**Follow-ups:**
- Author root `README.md` per acceptance criteria (purpose, disclaimer, PRD links).
- Decide and document commit-signing policy.

---

## Status — 2026-05-16

Re-audit of the root `README.md` (~141 lines).

**Implemented (new since 2026-05-15):**
- Root `README.md` opens with project name "Casa Segura", one-sentence purpose ("Automated analysis of Salvadoran real estate contracts…"), BR-07 disclaimer block containing the literal "Esto no es asesoría legal", and links to formal PRDs (`docs/Casa Segura Formal PRDs/PRD_GENERAL.md`, FEATURES_MAP, Architecture, Domain model) and to the Roadmap. AC3 ticked.
- ADR index now lives in `docs/adr/` directory linked from README (`ADR-0001` Django backend, `ADR-0002` `platform_core` rename, `ADR-0003` CS-031 normalization, `ADR-0004` versioning). README also links to `backend/README.md`, `frontend/README.md`, and `RAILWAY.md` for service-level guides.

**Partial / Missing:**
- AC1 (public repo URL resolves without auth): cannot verify from local clone — must be confirmed against GitHub posture by maintainer.
- AC5 (commit signing): no policy documented; recent commits not GPG-signed.

**Flip:** stays `in_progress`. 3/5 ACs ticked. Remaining gaps are environmental (public-repo verification) and policy (commit signing), not code/docs.


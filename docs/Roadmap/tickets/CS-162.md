---
id: CS-162
epic: EPIC-06
status: done
owner: tbd
estimate: M
depends_on:
  - CS-158
prd_refs:
  - RUBRICA_CONTRATO §7, §16 (D1–D6)
domain: backend
secondary_domains:
  - ai-rag
tags:
  - casa-segura
  - ticket
  - epic-06
  - domain/backend
  - domain/ai-rag
---

# CS-162 — Category D evaluators (D1–D6)

## Context

Category **D** models property/registry risk declared in text: status disclosure, Bien de Familia / IVU pattern, FSV preventive annotation, permits, responsible professional, subdivision regularization ([[RUBRICA_CONTRATO]] §7). Overrides **art_3_ivu_family_homestead** (D2) and **art_58_fsv** (D3 narrative). D6 remains **`provisional_pending_corpus`** per rubric §7 — evaluator must surface tag without inventing statutory text.

## Scope

**In:**
- Evaluators `D1`…`D6` with explicit `requires_external_verification` flags bubbling to findings where rubric demands user/lawyer action
- Detection heuristics for IVU-linked vocabulary triggering D2 scope ([[RUBRICA_CONTRATO]] D2 detection list)
- FSV non-consent sale scenario mapping to **art_58_fsv**

**Out (explicit):**
- Registry API polling — MVP exclusion ([[PRD_GENERAL]] §2)

## Acceptance criteria

- [ ] Given contract omits registry status entirely, D1 returns **0** ([[RUBRICA_CONTRATO]] D1 scale)
- [ ] Given Bien de Familia transfer attempt without cancellation deed fixture, D2 triggers **art_3_ivu_family_homestead**
- [ ] Given preventive FSV annotation declared without Fund consent letter, D3 scores **0** with `art_58_fsv`
- [ ] Given construction permit stale >1 year without continuity evidence, D4 maps to **0**
- [ ] Given D6 provisional flag from catalog, evaluation metadata includes `provisional_pending_corpus=true` and avoids fabricated citations

### BVA — boundary value analysis

| ID | Boundary | Expected |
|---|---|---|
| D1 | attachment proof present vs free-lien oral statement | 10 vs 7 vs 3 vs 0 |
| D2 | regime lapsed vs active transfer | 10 vs 0+override |
| D3 | silence vs explicit no annotation vs blocked sale | 5 unverifiable vs 10 vs 0 |
| D4 | dated permit vs dubious format | 10 vs 6 vs 3 |
| D5 | full professional ID vs generic contractor | 10 vs 3 vs 0 |
| D6 | registered lot vs unknown regularization | 10 vs 0 provisional |

## Test intent (Rule 9)

D3/D2 overrides tie to **absolute transaction validity** — tests must keep `art_58_fsv` separate from generic yellow findings ([[STATUS_RECONCILIATION]] scoring deltas).

## Notes

- Align user messaging with [[PRD_GENERAL]] limitations §15 mirror in rubric Known limitations.

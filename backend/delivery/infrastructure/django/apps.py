from django.apps import AppConfig


class DeliveryConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "delivery.infrastructure.django"
    label = "delivery"
    verbose_name = "Casa Segura — Multichannel Delivery"

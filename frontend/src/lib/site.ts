/** Visible contact / source URL for crawlers (Meta, etc.). Update if the canonical repo moves. */
export const PUBLIC_SOURCE_REPO_URL =
  "https://github.com/Casa-Segura/casa-segura" as const;

/** App mark — keep in sync with `docs/assets/casa-segura-logo.png`. */
export const BRAND_LOGO_PATH = "/brand/casa-segura-logo.png" as const;

export const BRAND_LOGO_ALT =
  "Casa Segura — marca de una casa estilizada sobre fondo verde azulado" as const;

/** Shared folder with sample contracts and media for manual QA (opens Google Drive in a new tab). */
export const PUBLIC_TEST_DATA_DRIVE_URL =
  "https://drive.google.com/drive/folders/1iTzumO-TPEulBXOxe4W6D0pTgGzWhtrl?usp=sharing" as const;
